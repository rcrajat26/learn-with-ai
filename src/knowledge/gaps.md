# Knowledge Gaps — per topic guide, evidence-based

> **Generated by** the gaps-analysis pass over `src/topics/` + all diagnostic
> evidence under `tmp/`. **Pass 2 (final reconciliation) complete** — reconciled
> against `src/knowledge/understanding.md`, which is itself final for now. Every
> topic below carries a cross-link to its ledger section; where the two files
> disagreed, the one with the more specific question-level citation won and the
> adjustment is recorded in place.
>
> **Evidence snapshot: 2026-08-21.** 140+ scored answers. Nothing here postdates
> that date, and **no paper has been taken since** — both files describe the same
> frozen state.
>
> **Paper inventory**
>
> | Instrument | State |
> |---|---|
> | Easy tier E1–E5 + code session | **Spent.** ~71% aggregate (70/99). Archived as study material. |
> | Medium M5, M1, M2 | **Spent**, all taken unprepped (3.5 / 6.5 / 3.0 of 19). |
> | Medium **M3, M4** | **Fresh and gated.** The last clean medium instruments. M3 = post-study verification (target ≥10/19), M4 = confirmation. Do not take either before the study plan is run. |
> | Hard paper 1 | **Benchmark only.** Not to be attempted until both M3 and M4 clear 14/19. Hard 2–5 were deleted as stale. |
> | Ad-hoc papers 1–3 | **Pending, untaken.** Breadth instruments for the unmeasured topics; deliberately below M3/M4 so they cost no fresh medium paper. |
>
> **Standing recommendation — unchanged and load-bearing:** run
> `tmp/primers/study-plan.md` top to bottom (7–10 focused days) plus the deferred
> code redo gate **before M3**. The single completed study→verify loop in the record
> (concurrency, 0.5 → 2.0) worked; all three unprepped medium measurements
> flatlined. Taking another paper first re-measures a known state and spends an
> instrument to do it.

**Scope:** one section per topic guide in `src/topics/` (01–20, same order).
Every claim here cites a specific paper question or valuation. Nothing is
inferred from self-assessment.

**Evidence base (all of it):**

| Instrument | Date | Result | Valuation |
|---|---|---|---|
| Easy paper 1 (E1) | 2026-07-11 | 14.5/20 | `tmp/valuations/E1-valuation.md` |
| Easy paper 2 (E2) | 2026-07-11 | 12.5/19 | `tmp/valuations/E2-valuation.md` |
| Easy paper 3 (E3) | 2026-07-12 | 14.5/20 | `tmp/valuations/E3-valuation.md` |
| Easy paper 4 (E4) | 2026-08-18 | 14.5/20 (+Q14b) | `tmp/valuations/E4-valuation.md` |
| Medium paper 5 (M5) | 2026-08-19 | 3.5/19 (accidental tier jump) | `tmp/valuations/M5-valuation.md` |
| Easy paper 5 (E5) | 2026-08-20 | 14.0/20 (+Q10b) | `tmp/valuations/E5-valuation.md` |
| Deferred code session | 2026-08-21 | 1/3 | `tmp/valuations/code-session-valuation.md` |
| Medium paper 1 (M1) | 2026-08-21 | 6.5/19 | `tmp/valuations/M1-valuation.md` |
| Medium paper 2 (M2) | 2026-08-21 | 3.0/19 | `tmp/valuations/M2-valuation.md` |

**Aggregate:** easy tier ≈ 71% (70/99 scored answers). Medium tier 13/57
across three papers, all taken **without** the prescribed between-tier study.
Not-yet-taken instruments: **M3, M4** (held for post-study verification),
**hard paper 1** (benchmark only).

**Status vocabulary**

- **MEASURED** — the topic's core mechanisms were probed at two tiers, or at
  one tier across ≥4 distinct questions, and the result is stable.
- **PARTIALLY MEASURED** — probed, but sampling hit one corner of the guide;
  the rest of the checklist is untouched.
- **UNMEASURED** — 0–2 questions ever, or the questions asked don't reach the
  guide's actual content.

**Severity** follows `tmp/qbank/13-scoring-and-report.md` §2:
CRITICAL / HIGH / MEDIUM / INFO.

**Remediation state** is one of: OPEN (no material written) · CHAPTERED
(written, unverified) · IN VERIFICATION (retest queued) · CLOSED (retest passed).

---

## 01 — DSA fundamentals

> **Understanding ledger:** `src/knowledge/understanding.md` §01 — score **L1.5**, measurement confidence **HIGH**. Reconciliation: agrees: PARTIALLY MEASURED here = "complexity reasoning plus write-fluency, not pattern selection" there.

**Status: PARTIALLY MEASURED.** Complexity theory was probed five times
(E1 Q1, E2 Q1, E3 Q2, E5 Q1–Q2, M2 Q1); write-fluency was probed once, in the
deferred code session (1/3). Pattern recognition beyond binary search and
hashing has **never** been asked.

### Confirmed gaps

**GAP: code write-fluency (the tier's widest theory-practice split)**
- Evidence: code session 1/3 — E2 Q2 answered as English pseudocode instead of
  Java; E4 Q2 submitted non-compiling (declared `Map<Character,Integer>`, no
  `return`) and omitted the explicitly-asked complexity statement; M5 Q1
  (longest substring, the only real algorithm problem) not attempted at all.
  Contrast: ~71% on the same tier's theory.
- Severity: **HIGH** (CRITICAL if it survives the redo gate — coding rounds are
  the primary filter at this YOE per `tmp/gaps.md` §1.1).
- Type: unknown-unknown promoted to confirmed; upgrades `tmp/gaps.md` §1.1 from
  anticipated to evidence-backed.
- Remediation: **IN VERIFICATION.** Redo gate defined in
  `tmp/valuations/code-session-valuation.md` (isPalindrome two variants,
  charFrequencies compiling with `merge` + complexity, the E5 SQL plus two fresh
  word problems, M5 Q1 with its 25-min timebox). Gate not yet run. Standing rule
  added: compile/run before submitting; mark reference lookups; budget to zero
  by medium tier.

**GAP: amortized analysis as a named concept**
- Evidence: M2 Q1 blank — while E4 Q1 (ArrayList grow-and-copy) scored 1. The
  mechanism is known; the vocabulary that interviews use for it is not.
- Severity: MEDIUM. Type: depth-illusion (adjacent form answered correctly).
- Remediation: OPEN. Covered by checklist item "amortized O(1) means the
  sequence averages constant" in `01-dsa-fundamentals.md`.

**GAP: heap / PriorityQueue internals**
- Evidence: M5 Q2 = 0 — `peek` O(1) guessed right for the wrong reason ("end of
  queue" rather than the array-backed heap's root), `poll` called O(n), iteration
  believed sorted.
- Severity: MEDIUM. Remediation: OPEN (listed on the M5 between-tier syllabus).

### Verified non-gaps / strengths

- Big-O of common operations, including the nested-loop triangular case
  (E1 Q1 = 1, E2 Q1 = 1).
- Binary search preconditions and log₂(10⁶) ≈ 20 (E3 Q2 = 1).
- Recursion: base case, progress, StackOverflowError (E5 Q2 = 1).
- Array vs linked-list trade-offs partially (E5 Q1 = 0.5) — the O(1) middle
  insert caveat and CPU cache locality were the misses.

### Unmeasured checklist items

Sliding window and its monotonic-constraint precondition · monotonic stack ·
`ArrayDeque` as the correct stack/queue (recall failed once at E1 Q2 but was
never retested) · fast/slow pointers and cycle-start recovery · BST validation
with propagated bounds · BFS shortest path and mark-visited-at-enqueue ·
`heapify` O(n) · binary search on the answer / "minimize the maximum" · no tail
recursion in the JVM · backtracking choose/recurse/un-choose · DP state
definition, knapsack iteration direction · greedy exchange argument · Dijkstra
vs Bellman-Ford · directed cycle detection via recursion stack · Kahn's ·
tries · union-find · **reading the input constraints to select a complexity
class** — i.e. essentially the entire pattern-recognition half of the guide.
**Ad-hoc paper 1 covers this.**

---

## 02 — Java collections

> **Understanding ledger:** `src/knowledge/understanding.md` §02 — score **L2**, measurement confidence **HIGH**. Reconciliation: agrees; ledger adds the recall-vs-reasoning framing for the class-name gap.

**Status: MEASURED (easy), PARTIALLY MEASURED (medium).** Eight distinct
questions across both tiers.

### Confirmed gaps

**GAP: HashMap constants and resize mechanics**
- Evidence: M1 Q2 = 0.5 — skeleton correct (bucket → chain → resize) but
  capacity said to grow 1.5× (that's ArrayList), load-factor 0.75 not named,
  treeify-at-8 unknown. E4 Q1 conflated the two growth factors in the other
  direction.
- Severity: MEDIUM. Type: confirmed-anticipated (`tmp/gaps.md` §3.4 stdlib drills).
- Remediation: CHAPTERED — `tmp/primers/quick-notes.md` §Java. Unverified.

**GAP: equals/hashCode contract stated as a contract**
- Evidence: M1 Q1 = 0.5 — "different buckets" mechanism half-present; the
  contract ("equal objects MUST return equal hashCodes") and the symptoms
  (`get(equalKey)` misses, logical duplicates coexist) absent.
- Severity: MEDIUM. Remediation: OPEN.

**GAP: ConcurrentModificationException**
- Evidence: M2 Q3 = 0, blank.
- Severity: MEDIUM ([CORE] item, interviews probe it directly).
- Remediation: OPEN.

**GAP: class-name recall vs reasoning**
- Evidence: E1 Q2 = 0.5 — correct queue/stack reasoning, invented
  `LinearQueue`; `ArrayDeque` not recalled.
- Severity: MEDIUM. Type: confirmed-anticipated (§3.4). Never retested.

### Verified non-gaps / strengths

- HashMap vs TreeMap ordering and complexities (E3 Q1 = 1).
- List/Set/Map properties and fitting examples (E5 Q3 = 1).
- ArrayList grow-and-copy plus pre-sizing rationale (E4 Q1 = 1).
- **`list.remove(10)` overload trap (M1 Q3 = 1)** — a medium-tier full mark;
  the Java-core spine holds a tier up.
- Rehash bucket-splitting insight offered unprompted (M1 Q2).

### Unmeasured checklist items

`Map` is not a `Collection` · `Arrays.asList` vs `List.of` · LinkedList vs
ArrayDeque · bucket index `hash & (capacity-1)` and the high-bit XOR ·
`new HashMap<>(n)` sizing · null-key rules across HashMap/CHM/Hashtable ·
LinkedHashMap access-order LRU + `removeEldestEntry` · TreeMap red-black and
`compareTo`-as-identity · NavigableMap floor/ceiling/subMap · Set-as-Map-wrapper,
EnumSet bit vector · `equals` overload-vs-override · `Iterator.remove`/`removeIf` ·
`unmodifiableList` view vs `List.copyOf` · `synchronizedMap` compound actions ·
`Comparator.reversed()` chain semantics · never-compare-by-subtraction ·
TimSort vs dual-pivot quicksort. **Ad-hoc paper 1 covers the highest-value subset.**

---

## 03 — Java core

> **Understanding ledger:** `src/knowledge/understanding.md` §03 — score **L2**, measurement confidence **HIGH**. Reconciliation: agrees; ledger files the recurring 0.5s as the same single "mechanism precision" gap.

**Status: MEASURED.** Eleven questions across both tiers; the most heavily
sampled topic after SQL.

### Confirmed gaps

**GAP: Integer cache**
- Evidence: M1 Q4 = 0.5 — predicted `false` for `127 == 127` on boxed values;
  the −128..127 cache is the entire point of the question.
- Severity: MEDIUM ([CORE], high interview frequency).
- Remediation: CHAPTERED — `tmp/primers/quick-notes.md`. Unverified.

**GAP: generics erasure and PECS**
- Evidence: M2 Q4 = 0. The M2 valuation groups it under "blank/unaware"; the raw
  answer file is more specific and `understanding.md` §03 cites it — the candidate
  wrote that erasure and PECS are **"entirely new"** to him. Recorded as
  self-declared-unknown rather than a skip, which matters because it is an honest
  signal, not a process failure. Note E5 Q4 (generics purpose) scored 1 —
  compile-time checking is understood, the runtime model is not.
- Severity: MEDIUM. Type: depth-illusion.
- Remediation: OPEN.

**GAP: mechanism-level precision on language rules**
- Evidence, a recurring 0.5 pattern rather than one item: E2 Q3 checked vs
  unchecked explained as "the compiler isn't aware of unchecked" (the real
  distinction is catch-or-declare **enforcement**); E2 Q4 attributed default
  methods to abstract classes as the differentiator (interfaces have had them
  since Java 8); E4 Q3 gave the *what* of `static` but not the asked *why*
  (no `this`); E1 Q4 missed that loop concatenation produces ordinary heap
  objects and costs O(n²).
- Severity: MEDIUM (cluster of [CORE] items at 1 — §2 of the scoring rules).
- Remediation: OPEN. This is the "mechanics-level precision" thread named in
  `tmp/gaps.md` §9's E4 entry.

**GAP: defensive copying in immutable design**
- Evidence: M5 Q3 = 0.5 — final class/fields/no setters present; the explicitly
  demanded defensive copies of mutable constructor params **and** getters absent.
- Severity: MEDIUM. Remediation: OPEN.

### Verified non-gaps / strengths

- `==` vs `equals` with correct pool-vs-heap reasoning (E1 Q3 = 1).
- `final` on variable/method/class **including** the reference-vs-contents trap
  on a final `List` (E3 Q3 = 1) — full-mark work.
- Primitives vs wrappers (E3 Q4 = 1).
- Generics' purpose and ClassCastException prevention (E5 Q4 = 1, self-flagged
  but correct — see the confidence-miscalibration finding).
- String immutability definition (E1 Q4, partial).

### Unmeasured checklist items

Integer overflow wrap and `Math.addExact` · unboxing NPE · `intern()` ·
StringBuilder default capacity/growth · `static final` inlining ·
try-with-resources reverse close order and suppressed exceptions ·
`return` inside `finally` · swallowing `InterruptedException` · array covariance
and `ArrayStoreException` · default-method resolution order · inner/anonymous
class enclosing-instance leaks · lambda capture of effectively-final locals ·
enum as singleton, never persist `ordinal()` · `clone` is broken / `finalize`
deprecated · **BigDecimal for money, `equals` vs `compareTo`, divide without
RoundingMode** · `SimpleDateFormat` thread-safety · `LocalDateTime` is not an
instant · `Duration` vs `Period` across DST · pass-by-value.
(BigDecimal and `Optional` are queued in **M4 Q3/Q4** — do not re-cover in an
ad-hoc paper.)

---

## 04 — Modern Java (8–21)

> **Understanding ledger:** `src/knowledge/understanding.md` §04 — score **L1**, measurement confidence **LOW**. Reconciliation: ledger lowered L1.5 → L1 in its pass 2 citing this section's HIGH severity on "everything after Java 8".

**Status: UNMEASURED.** Three questions total, one of which the candidate
self-flagged as unknown. This is the least-measured topic with real interview
weight.

### Confirmed gaps

**GAP: everything after Java 8**
- Evidence: M5 Q4 = 0.5 — records and `Optional` described with uses (that
  half was fine); **sealed interfaces, pattern-matching switch, virtual
  threads, and text blocks all self-flagged unknown**. Streams/functional
  interfaces were offered as "modern," which dates the mental model to 2014.
- Severity: **HIGH** — `04-modern-java.md` is a core guide, the plan's Week 15
  treats this as asked-at-every-level material, and virtual threads are a
  standard 2026 Java screen.
- Type: confirmed-anticipated (`tmp/gaps.md` M5 entry: "reinforces Week 15 as
  first-pass learning").
- Remediation: OPEN — no chapter written.

**GAP: `Optional`'s purpose**
- Evidence: E3 Q8 = 0, "not sure."
- Severity: MEDIUM. Remediation: **IN VERIFICATION** — M4 Q3 retests it at
  depth (correct use + two abuses + `orElse` vs `orElseGet`).

### Verified non-gaps / strengths

- Stream pipeline prediction: `map(toLowerCase) → filter → distinct` traced
  correctly to output `1` (E4 Q4 = 1).
- Records: purpose and use stated correctly (M5 Q4, the scoring half).

### Unmeasured checklist items

Lambda → `invokedynamic`, not an anonymous class · `this` inside a lambda ·
stream laziness and fusion · single-consumption · `peek` may be skipped ·
`Collectors.toMap` duplicate-key/null throw · `groupingBy` default map/list
types · `toList()` unmodifiable vs `collect(toList())` · parallel streams on
the common pool and when parallel is even valid · `orElse` eager evaluation
(M4 Q3 pending) · Optional-as-field/param abuse (M4 Q3 pending) · `var`
inference · record compact constructors and shallow immutability · sealed +
exhaustive switch · pattern-switch NPE on null without `case null` · text-block
indentation rule · switch expressions · **virtual thread unmount mechanism,
pinning on `synchronized`, don't-pool, no CPU-bound help, semaphore for
backpressure** · structured concurrency vs `allOf` · sequenced collections.
**Ad-hoc paper 1 covers this heavily.**

---

## 05 — Multithreading & concurrency

> **Understanding ledger:** `src/knowledge/understanding.md` §05 — score **L1.5**, measurement confidence **HIGH**. Reconciliation: agrees; ledger counts 11 probes to this file's 13 (attribution only).

**Status: MEASURED at both tiers.** The most-instrumented topic: 13 questions,
and the only completed study→verify loop in the whole exercise.

### Confirmed gaps

**GAP: volatile vs atomicity (repeat regression)**
- Evidence: E5 Q5 = 0.5 — `counter++` on a volatile field called thread-safe,
  **after** `tmp/primers/concurrency-primer.md` §3 covered it verbatim. Then
  M1 Q5 = 0.5 — the volatile examples couldn't be produced. **Two misses on the
  same item post-remediation.** M5 Q5 = 0.5 adds the DCL angle: the volatile
  requirement is known as visibility only; the reordering / half-constructed
  object story is absent.
- Severity: **HIGH** — [CORE], interview-frequent, and remediation has already
  failed once.
- Remediation: **IN VERIFICATION** — `tmp/primers/fundamentals-primer-2.md`
  part 4 sharpens it; M4 Q5 (three fixes for `counter++`) is the queued retest.

**GAP: compound actions on concurrent collections**
- Evidence: M1 Q6 = 0 — `containsKey`+`put` race answered "make it async,"
  which doesn't touch the bug. The insight being tested (a concurrent collection
  makes single ops atomic, never compound ones) is absent; `computeIfAbsent`
  not reached.
- Severity: **HIGH** — this is the medium-tier concurrency floor and it sits
  directly on the repaired basics.
- Remediation: OPEN. `tmp/gaps.md` M1 entry promotes qbank 03 from measurement
  to study material.

**GAP: thread pool mechanics**
- Evidence: E2 Q5 = 0 ("not aware"); M2 Q6 = 0 (ThreadPoolExecutor, blank).
  Never scored above 0 in two attempts a tier apart.
- Severity: **HIGH** — core-then-queue-then-max ordering, unbounded-queue OOM,
  and rejection policies are standard Java-backend screens.
- Remediation: CHAPTERED — concurrency primer §4 exists but has produced no
  measured improvement (M2 Q6 postdates it).

**GAP: deadlock explanation (recognition without articulation)**
- Evidence: E3 Q6 = 0.5 (answer file contained an unfilled
  `***ADD ANSWER HERE***` placeholder); M2 Q5 = 0.5 — deadlock **recognized on
  sight**, but the two-sentence explanation (T1 holds A wants B, T2 holds B
  wants A → circular wait; fix = global lock ordering) never came.
- Severity: MEDIUM (recognition is present; articulation is the gap).
- Remediation: CHAPTERED — concurrency primer §6, verbatim. Unverified twice.

**GAP: in-memory state in a singleton as a distributed-systems bug**
- Evidence: M2 Q7 = 0.5 — the race was identified and "synchronized list"
  proposed; the larger problem (pending work held in a singleton is lost on
  restart and wrong with >1 instance; persist it instead) was missed.
- Severity: MEDIUM.

### Verified non-gaps / strengths — the remediation proof

- **Section trajectory 0.5 → 0.5 → 1.5 → 2.0 → 1.0** across E1–E5. The primer
  is the only intervention in the whole record with a measured effect.
- Process vs thread and heap/stack sharing: E1 Q5 = 0 → **E3 Q5 = 1** (direct
  retest of the zero).
- `start()` vs `run()`: E4 Q5 = 1, hedged but correct.
- Race-condition definition and example (E1 Q6, partial — the read-modify-write
  decomposition was the miss).
- `synchronized` mutual exclusion (E2 Q6, M1 Q5) — consistently half-marked
  because the **visibility** guarantee is consistently omitted.

### Unmeasured checklist items

Thread states BLOCKED vs WAITING partially probed (E5 Q6 = 0.5) but socket-I/O-
reports-RUNNABLE never asked · never lock on a String literal / boxed Integer /
reassignable field · the seven happens-before edges · CAS retry loops, LongAdder
under contention (M4 Q5 pending) · ABA and `AtomicStampedReference` · the four
Coffman conditions by name · `newFixedThreadPool`'s unbounded queue →
maximumPoolSize is dead · `CallerRunsPolicy` as backpressure · uncaught exception
cancels all future scheduled runs · `CompletableFuture` common-pool default and
silent exception swallowing · `SynchronousQueue` zero capacity · **ThreadLocal
pool leak** (M4 Q6 pending) · safe publication · CHM per-bin locking, weakly
consistent iterators, approximate `size()`, no-nulls rationale ·
`computeIfAbsent` mapping function runs under the bin lock · always `wait()` in
a `while` · `ReentrantLock` unlock-in-finally, tryLock, fairness, Conditions ·
CountDownLatch vs CyclicBarrier vs Semaphore.

---

## 06 — JVM internals

> **Understanding ledger:** `src/knowledge/understanding.md` §06 — score **L1**, measurement confidence **LOW**. Reconciliation: divergent by design: L1 there (4 probes clear its NONE bar), UNMEASURED here (status, not level) — see the note below.

**Status: UNMEASURED.** Three questions, all conceptual; the guide's entire
diagnostic half has never been touched.

### Confirmed gaps

**GAP: the OOM taxonomy**
- Evidence: M5 Q6 = 0 — StackOverflowError offered as a kind of OOM; the three
  real ones (heap space, unable-to-create-native-thread, Metaspace) not named,
  `-XX:+HeapDumpOnOutOfMemoryError` unknown.
- Severity: **HIGH** — "read the text after `OutOfMemoryError:`" is a [CORE]
  diagnostic item and `tmp/gaps.md` §3.1 already flags the debugging toolchain.
- Remediation: OPEN.

**GAP: OOMKilled vs OutOfMemoryError**
- Evidence: M5 Q19 = 0, blank. (Also counted under topic 19.)
- Severity: HIGH — exit 137 vs a stack trace have opposite fixes, and this is a
  standard container-era screen.
- Remediation: OPEN.

### Verified non-gaps / strengths

- Heap vs stack allocation and the two corresponding errors (E3 Q5 = 1) — the
  repaired E1 zero.
- Garbage collection basics: unreachability, eligibility, `System.gc()` is a
  hint (E4 Q6 = 1).

**Not a contradiction (reconciliation).** `understanding.md` §06 records **L1** with
LOW confidence while this file records **UNMEASURED**. The two are measuring
different things and both are correct: L1 is a *level* placed on four probes that
agree with each other (two conceptual passes, two diagnostic blanks); UNMEASURED is a
*status* about instrument coverage, driven by the fact that the guide's largest
sub-area — the whole jcmd/jstack/jmap/jstat/MAT toolkit — has never been asked by any
paper. Read them together as "what little was sampled came back at L1, and the part
that matters most for interviews was never sampled." Ad-hoc paper 1 Sections 5–6
resolve both at once.

### Unmeasured checklist items

Metaspace in native memory / PermGen removal · **total process memory = heap +
metaspace + code cache + stacks + direct buffers**, never `-Xmx` = container
limit · generational hypothesis, Eden→Survivor→tenuring · **G1 as the default,
region-based, pause-goal driven; humongous objects** (M3 Q6 pending) · ZGC ·
`-Xms` = `-Xmx`, GC logging, never `System.gc()` · `GC overhead limit exceeded` ·
`ExitOnOutOfMemoryError` · classloader delegation, class identity = name +
loader · **`ClassNotFoundException` vs `NoClassDefFoundError`** ·
classloader leaks · JIT tiers C1/C2, deoptimization, **warmup after deploy** ·
JMH · **`jcmd`/`jstack`/`jmap`/`jstat`/JFR** · the 100%-CPU workflow
(`top -H` → hex TID → three jstacks → match `nid`) · the leak workflow
(post-full-GC floor → histogram diff → heap dump → MAT dominator tree →
path to GC roots) · retained vs shallow size · the unbounded-map-in-a-singleton
Spring leak · `MaxRAMPercentage` default 25% → set ~70 · Native Memory Tracking.
**Ad-hoc paper 1 covers the diagnostic toolkit.**

---

## 07 — Spring core

> **Understanding ledger:** `src/knowledge/understanding.md` §07 — score **L1.5**, measurement confidence **HIGH**. Reconciliation: agrees; this file sharpens "highest-leverage" to CRITICAL severity.

**Status: MEASURED.** Eight questions; the medium tier produced the single
highest-leverage finding in the record.

### Confirmed gaps

**GAP: the Spring proxy model — the highest-leverage item on the board**
- Evidence: **M1 Q7 = 0 (blank) and M1 Q8 = 0 together.** Q8's prediction was
  wrong in the dangerous direction: `this.audit(o)` was expected to honour
  `REQUIRES_NEW`, when self-invocation bypasses the proxy entirely and the audit
  silently joins the outer transaction. Proposed fix ("make it async/
  non-transactional") changes the requirement instead of fixing the mechanism.
- Severity: **CRITICAL** — L0 in a daily-use framework area (scoring rules §2),
  and it gates `@Transactional`/`@Cacheable`/`@Async`/AOP questions across
  M2–M4, the plan's Day 55–65 JSD-D track, and real interviews.
- Type: confirmed-anticipated — this formally confirms `tmp/gaps.md` §2.1
  ("foundations before sharp edges") at medium tier, converting it from
  inference to evidence.
- Remediation: **CHAPTERED, unverified** — `tmp/primers/study-plan.md`
  Day 1–2 block. Retests: M3 Q7/Q8, M4 Q7/Q8.

**GAP: `@Repository`'s behaviour**
- Evidence: E1 Q8 = 0.5 — "semantic markers, same mechanics" is right for
  `@Component`/`@Service` but misses that `@Repository` is the one stereotype
  with behaviour: persistence-exception translation to `DataAccessException`.
- Severity: MEDIUM. Remediation: OPEN.

**GAP: what `@Transactional` *starts***
- Evidence: E3 Q7 = 0.5 — both endings (all-commit / all-rollback) captured; the
  question's first clause (a DB transaction is opened or joined before the
  method body) unanswered. Counted as the 4th asked-instance occurrence.
- Severity: MEDIUM.

### Verified non-gaps / strengths

- Dependency injection and constructor injection: IoC, decoupling, testability,
  dependencies-resolved-before-creation — all four (E1 Q7 = 1).
- Spring Boot starters and auto-configuration + embedded Tomcat (E2 Q7 = 1).
- Profiles: profile-overrides-base-when-active (E4 Q7 = 1).
- `@RestController` = `@Controller` + `@ResponseBody`, JSON vs view, Jackson
  (E5 Q7 = 1).

### Unmeasured checklist items

The two container phases · the four arguments for constructor injection stated
as arguments · field injection's reflection/`setAccessible` and hidden cycles ·
singleton mutable state as a data race (touched obliquely at M2 Q7) · prototype
captured in a singleton and the `ObjectProvider`/`@Lookup`/scoped-proxy fixes ·
CGLIB vs JDK proxies and why `final`/`private`/`static` can't be advised ·
ranking the self-invocation fixes · AOP proxies created in
`postProcessAfterInitialization`, so `@PostConstruct` is pre-proxy ·
**the default rollback rule — checked exceptions commit** ·
`UnexpectedRollbackException` · `REQUIRES_NEW` (second connection) vs `NESTED`
(savepoint) · `readOnly = true` → flush mode MANUAL ·
`AutoConfiguration.imports` + `@ConditionalOnMissingBean` · `--debug` conditions
report · `@ConfigurationProperties` over `@Value` · property precedence
(env vars outrank yml) · the DispatcherServlet flow · never return an entity
from a controller · `@EventListener` synchronous vs
`@TransactionalEventListener(AFTER_COMMIT)` · `@Async` exception swallowing and
no ThreadLocal propagation · filter vs interceptor vs aspect · `@Scheduled`
single thread and runs-on-every-replica · circular deps disabled since Boot 2.6.

---

## 08 — Spring Data JPA

> **Understanding ledger:** `src/knowledge/understanding.md` §08 — score **L1**, measurement confidence **HIGH**. Reconciliation: agrees; ledger calls L1 its largest single delta against the reference profile.

**Status: PARTIALLY MEASURED.** Six questions; the easy tier only reached
annotations, and every medium-tier probe came back blank.

### Confirmed gaps

**GAP: JPA sharp edges — LazyInitializationException and N+1**
- Evidence: **M5 Q7 = 0 and M5 Q8 = 0, both blank.** These are the two
  highest-value JPA interview topics at this YOE.
- Severity: **CRITICAL** — L0 in a daily-use area.
- Type: confirmed-anticipated (`tmp/gaps.md` §2.1).
- Remediation: CHAPTERED as a syllabus line in `tmp/primers/study-plan.md`
  Day 1–2; no written chapter. Retest: M4 Q7 (first-level cache + the 500k-entity
  OOM), M4 Q8 (`merge` on a detached entity).

**GAP: entity lifecycle states**
- Evidence: M2 Q8 = 0, blank. `tmp/gaps.md` §2.1 predicted exactly this
  ("transient/managed/detached/removed, and what persist/merge/flush do").
- Severity: **CRITICAL** — same reasoning; it is the prerequisite that makes
  L1-cache and dirty-checking material land.
- Remediation: CHAPTERED (study-plan Day 1–2). Retest: M4 Q7/Q8.

**GAP: fetch-type defaults**
- Evidence: E4 Q8 = 0.5 — LAZY and EAGER defined correctly, but which
  association type defaults to which was unknown. This is the root of half of
  all N+1 stories and connects directly to the two blanks above.
- Severity: HIGH ([CORE], top-3 JPA screen).
- Remediation: OPEN.

### Verified non-gaps / strengths

- Entity mapping basics: `@Entity`, `@Id`, `@GeneratedValue` (E2 Q8 = 1).
- Schema migrations rationale (E5 Q8 = 0.5) — "control + trackable" present;
  versioned-ordered-scripts-with-recorded-history and why `ddl-auto=update` is
  dangerous were the misses.

### Unmeasured checklist items

The persistence context as identity map + dirty-checking snapshot store ·
`save()` unnecessary on a managed entity · flush ≠ commit, auto-flush before
JPQL but not native · `persist` mutates the argument, `merge` returns a managed
copy (M4 Q8 pending) · Spring Data `save()` and `isNew()` · non-owning
`@OneToOne` laziness and `@MapsId` · `getReference()` and far-away
`EntityNotFoundException` · JOIN FETCH / `@EntityGraph` / DTO projection /
`@BatchSize` · `MultipleBagFetchException` · `HHH000104` in-memory pagination ·
entity `hashCode` with generated ids, never Lombok `@Data` on an entity ·
`@Version` optimistic locking and retry · `PESSIMISTIC_WRITE` =
`SELECT ... FOR UPDATE` · L1 vs L2 cache (M4 Q7 pending) · batching with
`flush()`+`clear()`, `hibernate.jdbc.batch_size`, IDENTITY disables batching ·
Flyway with `ddl-auto=validate` · `@Modifying` bypasses the persistence context ·
`open-in-view` off.

---

## 09 — SQL & databases

> **Understanding ledger:** `src/knowledge/understanding.md` §09 — score **L1.5**, measurement confidence **HIGH**. Reconciliation: agrees; ledger counts 19 probes to this file's 16 (E5 Q8 and M1 Q14 filed elsewhere here).

**Status: MEASURED.** The most-sampled topic (16 questions) and the most
consistently weak theme across the whole easy tier.

### Confirmed gaps

**GAP: ACID — use-it-can't-define-it**
- Evidence: E4 Q10 = 0 (I and D blank, A and C misdefined) while `@Transactional`
  behaviour was operationally understood at E3 Q7. Retest E5 Q10b = 0.5:
  Atomicity now anchored, but the **Isolation↔Consistency swap persists** and
  Durability is still unmapped.
- Severity: **HIGH**, and explicitly still OPEN after one partial retest.
- Type: depth-illusion.
- Remediation: **IN VERIFICATION** — `tmp/primers/fundamentals-primer-2.md`
  part 1 written with the exact scenario mapping that was missed. Next retest:
  M3 Q10 (read anomalies).

**GAP: SQL word-problem recognition (unprompted problem shape)**
- Evidence: the sharpest single finding in the record. Code session E5 Q10 = 0 —
  "total per customer exceeding 10,000" answered with a row-level
  `WHERE amount > 10000`, no `SUM`, no `GROUP BY`, no `HAVING`, `FROM table`,
  unbounded date range, ordered by row amount. **Yet E2 Q9/Q10 produced correct
  GROUP BY/HAVING when the question named the operations.** Prompted recall
  exists; unprompted recognition does not.
- Severity: **HIGH** (CRITICAL if it survives the redo gate — SQL screens are
  common in fintech and the candidate's sector).
- Type: unknown-unknown; upgrades `tmp/gaps.md` §2.2 to evidence-backed and
  redefines the remediation shape: **word-problem reps, not concept review.**
- Remediation: IN VERIFICATION — redo gate includes the E5 SQL plus two fresh
  variants ("2025 order COUNT exceeds 5", "each customer's LARGEST single order
  in 2025"). Gate not yet run.

**GAP: index mechanics beyond "faster and costs space"**
- Evidence: E3 Q10 = 0.5, self-flagged — range/sort/join speedups missing, and
  the write-maintenance cost (every INSERT/UPDATE/DELETE maintains the index)
  missing. M1 Q10 = 0.5 — **the leftmost-prefix rule was inverted**:
  `WHERE customer_id = 42` on `(customer_id, created_at)` was judged unusable.
  B-trees described as "group hashes." M2 Q10 = 0 — planner skip reasons blank.
- Severity: **HIGH** — stated interview tiebreaker at this YOE, three
  consecutive misses.
- Remediation: partially CHAPTERED (`quick-notes.md` has the leftmost-prefix
  rule); study-plan Day 4–5.

**GAP: NULL semantics**
- Evidence: E4 Q9 = 0.5 — `WHERE middle_name = NULL` believed to throw; it
  silently returns zero rows, and the silence is the danger. M2 Q9 = 0 —
  `NOT IN` with a NULL in the subquery, blank.
- Severity: MEDIUM. Remediation: OPEN.

**GAP: transactions and concurrency anomalies**
- Evidence: M5 Q10 = 0 (lost update, blank).
- Severity: HIGH — pairs with the still-open ACID gap.
- Remediation: IN VERIFICATION — M3 Q10 covers dirty read / non-repeatable read /
  phantom.

**GAP: keyset vs offset pagination**
- Evidence: M5 Q9 = 0, blank; M1 Q14 = 0, blank (API-side framing).
- Severity: MEDIUM. Remediation: CHAPTERED — `tmp/primers/primer-3.md` §3
  (quick form); full treatment still on the syllabus.

### Verified non-gaps / strengths

- WHERE vs HAVING as concepts (E1 Q9, E2 Q10) — both defined correctly; only
  the execution-order clause and the alias-in-HAVING portability rule were missed.
- PK / FK / unique constraints and what each enforces (E3 Q9 = 1).
- DELETE vs TRUNCATE vs DROP, including selectivity (E5 Q9 = 1).
- INNER vs LEFT JOIN as a concept (E1 Q10 = 0.5 — the concrete asked instance
  was the miss, not the concept).

### Unmeasured checklist items

A `WHERE` predicate on the outer table of a LEFT JOIN silently makes it INNER
(M3 Q9 pending) · logical execution order and why an alias works in ORDER BY but
not HAVING · `COUNT(*)` vs `COUNT(col)`, `SUM` of nothing is NULL · window
functions: ROW_NUMBER vs RANK vs DENSE_RANK, top-N-per-group needing a CTE
(M4 Q9 pending) · `<> 'x'` excludes NULLs · UNIQUE constraint as the only
race-free duplicate prevention · B-tree page-read count · covering and partial
indexes · reading `EXPLAIN ANALYZE` estimated-vs-actual · seq vs index vs bitmap
heap scan · isolation-level defaults (Postgres READ COMMITTED, MySQL REPEATABLE
READ) · snapshot isolation, write skew, SERIALIZABLE retry · the three lost-update
fixes · `FOR UPDATE SKIP LOCKED` as a job queue · deadlock as a retryable error
with lock ordering as prevention · **Hikari pool sizing math and pool deadlock** ·
replication lag breaking read-your-writes · MVCC row versions and VACUUM bloat ·
expand/contract migrations, `CREATE INDEX CONCURRENTLY`, `lock_timeout` ·
UPSERT without a race (M4 Q10 pending).

---

## 10 — Networking

> **Understanding ledger:** `src/knowledge/understanding.md` §10 — score **L1.5**, measurement confidence **HIGH**. Reconciliation: agrees; L1.5 there is exactly the breadth-without-depth split recorded here.

**Status: MEASURED (breadth), UNMEASURED (depth).** Thirteen questions, but they
cluster on definitions; every mechanism question came back partial or blank.

### Confirmed gaps

**GAP: the TCP and TLS handshakes**
- Evidence: E5 Q11 = 0.5 — the URL→page walk went DNS straight to "hit the
  server on 443," skipping both handshakes. M1 Q11 = 0.5 — same walk, both
  handshakes **honestly flagged unknown**. M2 Q12 = 0 — TLS's three goals, blank.
  E3 Q11 = 0.5 — HTTPS answered with "MiM I guess" rather than the two concrete
  attacker abilities (read everything, modify traffic).
- Severity: **HIGH** — four probes, no improvement, and the URL→page walk is the
  single most-asked networking question in existence.
- Remediation: OPEN.

**GAP: ephemeral port exhaustion**
- Evidence: M2 Q11 = 0 — diagnosed as "thread pool exhausted"; the actual
  mechanism is `TIME_WAIT` ephemeral-port exhaustion, fixed by connection
  pooling / keep-alive. Wrong mechanism confidently given.
- Severity: MEDIUM.

**GAP: timeout defaults**
- Evidence: M1 Q12 = 0.5 — connect vs read distinguished correctly, but what
  each failure *tells you* was thin, and the crucial fact was inverted: **many
  Java clients default to no timeout at all**, which is why hangs happen.
- Severity: MEDIUM. Remediation: CHAPTERED — `quick-notes.md` §Networking.

**GAP: C10K / epoll**
- Evidence: M5 Q12 = 0 — "threads take turns" describes the thread-per-connection
  model the question asked to *replace*.
- Severity: INFO→MEDIUM — the plan teaches this at Day 104, but it is also the
  conceptual base for virtual threads (topic 04, already HIGH).

**GAP: WebSocket / SSE / webhook terminology**
- Evidence: M5 Q11 = 0.5 — a payment-completion callback (a webhook) was called
  SSE; the WebSocket HTTP-Upgrade handshake wasn't named.
- Severity: MEDIUM (cheap, interview-visible).

### Verified non-gaps / strengths

- TCP vs UDP with correct guarantees and correct example protocols (E1 Q11 = 1).
- IP vs port with 80/22 examples — scored above the key's bar (E2 Q12 = 1).
- `localhost` / loopback (E4 Q11 = 1).
- DNS resolution chain with TLD and A-record walk (E5 Q11, M1 Q11 — the DNS half
  was consistently the good half).
- Firewall definition (E5 Q12, partial) — the two concrete causes (security group
  blocking 8080, app bound to 127.0.0.1 not 0.0.0.0) were the miss.

### Unmeasured checklist items

TCP as a byte stream with no message boundaries (framing) · SYN backlog vs accept
queue · connection refused (RST) vs timeout · **JVM DNS caching pinning a dead IP
after failover** (M4 Q11 pending) · forward secrecy via ECDHE · cert chain and
`PKIX path building failed` · missing intermediates · SNI · TLS 1.3 1-RTT and
0-RTT replay · CNAME at the zone apex and Route 53 Alias · lowering TTLs before a
migration · why DNS failover is unreliable · HTTP/1.1 pipelining and HOL blocking
(M3 Q11 pending) · HTTP/2 multiplexing and **TCP-level** HOL · HTTP/2 + L4 LB
pinning · HTTP/3 / QUIC · the four timeouts plus a total budget · retry
amplification with depth · `TIME_WAIT` arithmetic (~470 conn/s) and
`EADDRNOTAVAIL` · `CLOSE_WAIT` as an fd leak · client idle timeout shorter than
the ALB/NAT idle timeout · kernel keep-alive 7200 s · sockets are fds and
`ulimit -n` · the 4-tuple · **L4 vs L7 balancing** · where TLS terminates ·
`X-Forwarded-For` · NAT state and NAT Gateway cost · CDN cache keys · versioned
URLs over purges · webhook receiver requirements · Netty event loops.

---

## 11 — Operating systems & Linux

> **Understanding ledger:** `src/knowledge/understanding.md` §11 — score **—** (no level placed), measurement confidence **NONE**. Reconciliation: ledger withdrew its L1 score to "—" in pass 2 citing this section's "None established".

**Status: UNMEASURED.** Two questions in nine papers, one of them a flat zero.
The guide is ~50 checklist items; the papers have touched three.

### Confirmed gaps

**GAP: hands-on Linux tooling**
- Evidence: E3 Q12 = 0, "not sure" — `top` for live per-process CPU/memory and
  `kill` (SIGTERM) vs `kill -9` (SIGKILL) are the cheapest interview points
  available and produced nothing. Consistent with E2's operational-depth dip
  (Q19/Q20 = 0.5 combined) while AWS *concepts* scored 2/2.
- Severity: **HIGH** — the scoring rules name debugging-methodology zeros as
  HIGH explicitly, and `tmp/gaps.md` §3.1 flags the toolchain.
- Type: unknown-unknown, now confirmed.
- Remediation: OPEN. The prescribed shape is **keyboard practice, not reading**:
  a 30-minute terminal drill (`top`, `htop`, `ps aux --sort=-%cpu`, `kill`,
  `tail -f`, `grep`). Retest queued: **M4 Q12 (production box triage)**.

**GAP: SSH authentication model**
- Evidence: E4 Q12 = 0.5 — purpose right; password vs public/private key pair,
  and why keys win for automation, missing.
- Severity: MEDIUM.

### Verified non-gaps / strengths

None established — the topic has never scored above 0.5.

**Reconciliation outcome.** `understanding.md` §11 carried **L1** in its first pass
and **withdrew the score to `—` (confidence NONE)** in its second, citing this line.
Its pass-1 L1 had leaned on evidence belonging to other topics: the process/thread
memory-model repair was retested at **E3 Q5** and is credited to §05 (concurrency),
E5 Q12's firewall question is §10, and M5 Q12's epoll question is §10. Stripping
those leaves exactly the two probes recorded here — E3 Q12 (tooling, 0) and E4 Q12
(SSH auth, 0.5). Both files now agree that no level can be placed.

### Unmeasured checklist items

Essentially the entire guide: process vs thread as `clone()` with different
sharing · a thread crash killing the process · Java platform threads 1:1 with
~1 MB stacks · thread-dump states and socket reads showing RUNNABLE · context
switch cost and `vmstat`'s `cs` column · pages, MMU, TLB, minor vs major faults ·
overcommit, VSZ vs RSS · page cache and reading `free -h`'s **available** column ·
**blocking syscalls burn threads not CPU** (M3 Q12 pending) · `D` state ·
zombies · fds and `ulimit -n` in containers, `/proc/<pid>/limits` · "Too many
open files" · `lsof -p` / `-i :port` / `+L1` · **`kill -3` for a free thread
dump** · graceful shutdown ordering · shutdown hooks never run on SIGKILL ·
Docker shell-form CMD swallowing SIGTERM · K8s deregistration racing SIGTERM and
the `preStop` fix · load average vs `nproc` · the `top` CPU split
(us/sy/wa/st/id) and high-`id`-with-bad-latency · `top -H` + hex TID → jstack ·
`df -h` vs `df -i` inodes · `df`/`du` disagreement = deleted-but-open file ·
`iostat -xz 1` `await` and `%util` · burst-credit exhaustion · `ps aux
--sort=-%mem` · the triage order · `tail -F` vs `-f` · `grep -C`/`-v`/`zgrep` ·
`sort | uniq -c | sort -rn` · `jq` · correlation IDs · cron `$PATH`, lost output,
`flock` · distributed cron double-runs · SSH key permissions, `ProxyJump`,
port forwarding, SSM · the kill workflow · capture dumps before killing ·
**OOM killer vs JVM OOM** · JVM RSS composition · `MaxRAMPercentage` · swapping.
**Ad-hoc paper 2 is dedicated to this.**

---

## 12 — API design

> **Understanding ledger:** `src/knowledge/understanding.md` §12 — score **L2**, measurement confidence **HIGH**. Reconciliation: ledger lowered L2.5 → L2 in its pass 2 citing this section's UNMEASURED-specifics status.

**Status: MEASURED (basics), UNMEASURED (design specifics).** Ten questions,
and this is one of the two strongest topics at easy tier — but every question
above the definition line came back blank.

### Confirmed gaps

**GAP: pagination design**
- Evidence: M1 Q14 = 0, blank; M5 Q9 = 0, blank (SQL-side framing).
- Severity: MEDIUM (HIGH in context — `tmp/gaps.md` §3.1 names pagination as
  part of the L4/L5 LLD round).
- Remediation: CHAPTERED — `primer-3.md` §3, quick form only.

**GAP: rate limiting**
- Evidence: M5 Q13 = 0, blank.
- Severity: MEDIUM. Remediation: OPEN.

**GAP: idempotency completeness**
- Evidence: E3 Q14 = 0.5 — definition and retry rationale good, but **PUT was
  omitted** from the list of idempotent methods, which is the canonical
  idempotent-write example. (Contrast M1 Q13 below.)
- Severity: MEDIUM, arguably closed by the M1 result.

### Verified non-gaps / strengths

- **PUT vs PATCH vs POST semantics and idempotency (M1 Q13 = 1)** — the best
  answer on that paper, including the PATCH-array-append nuance that makes PATCH
  non-idempotent. A medium-tier full mark.
- 401 vs 403 vs 404 (E1 Q13 = 1); status-code classes (E2 Q11 = 1).
- CRUD → HTTP verb mapping including collection vs item (E1 Q14 = 1).
- GET vs POST across caching, body, idempotency (E2 Q13 = 1).
- Path vs query vs body placement with rationale (E4 Q13 = 1).
- Async work → accepted-for-processing response (E2 Q16 = 1; `202 Accepted` +
  status URL is the crisp form).

### Unmeasured checklist items

Resources as plural nouns with the verb in the method · safe vs idempotent as
separate properties · `201` + `Location`, `202` + status URL · `409` for state
conflicts, `429` + `Retry-After` · never 200-with-an-error-body · modelling
non-CRUD actions · **end-to-end idempotency keys** (client UUID, insert-first,
work+response in one transaction, replay on COMPLETED, 409 on IN_PROGRESS,
request-hash mismatch) · server-side max page size and deterministic total
ordering · **URI vs header vs query versioning; additive changes avoid
versioning** · the deprecation sequence and `Deprecation`/`Sunset` headers ·
**RFC 7807 `application/problem+json`**, stable machine-readable type, trace id,
never leak stack traces, return all validation errors at once · token bucket and
distributed rate limiting · rate-limit key choice · never secrets in a query
string · `ETag`/`If-None-Match` → 304, `If-Match` → 412 · HATEOAS and why teams
skip it · choosing polling vs SSE vs WebSocket vs webhooks · webhook delivery
(retries with backoff, HMAC + timestamp, event ids, replayable log).
**Ad-hoc paper 3 covers this.**

---

## 13 — Web security

> **Understanding ledger:** `src/knowledge/understanding.md` §13 — score **L1.5**, measurement confidence **HIGH**. Reconciliation: agrees; ledger absorbed this file's CORS escalation (retest already failed once at M2 Q14).

**Status: MEASURED.** Nine questions across both tiers; contains the record's
one fully closed misconception and one still-inverted model.

### Confirmed gaps

**GAP: CORS enforcement side inverted**
- Evidence: E5 Q14 = 0.5 — the **browser** enforces CORS; the server merely
  declares policy via `Access-Control-Allow-*`. The candidate said server, while
  correctly intuiting that curl sails through — the intuition and the model
  contradict each other. M2 Q14 = 0, blank — and that question was named in the
  primer as its own retest.
- Severity: MEDIUM → HIGH on repetition (the chapter existed and the retest
  still scored 0; this is a study-execution failure, not a knowledge one).
- Remediation: **CHAPTERED, retest failed once** —
  `fundamentals-primer-2.md` part 3. Needs a fresh retest.

**GAP: JWT validation**
- Evidence: M2 Q13 = 0, blank.
- Severity: HIGH — [CORE], and stateless auth is the default in the candidate's
  stack.
- Remediation: OPEN. (Plan Week 15 / Day 72–73 territory; `tmp/gaps.md` §2.1
  flags session-vs-stateless as a missing prerequisite.)

**GAP: OAuth2 flows**
- Evidence: M5 Q14 = 0, blank (client credentials).
- Severity: MEDIUM (INFO-adjacent — the plan teaches this from scratch at
  Day 73, and it was expected blank pre-plan).

**GAP: prepared statements as a mechanism**
- Evidence: E4 Q14 = 0.5 — the concept was sketched (malicious input reaching
  the WHERE clause) but the question asked to *show* the vulnerable and safe
  one-liners, and `PreparedStatement` never appeared by name.
- Severity: HIGH ([CORE] item explicitly named in the scoring rules).
- Remediation: IN VERIFICATION — M4 Q14 asks for the mechanism directly.

### Verified non-gaps / strengths

- **Password storage: the record's one fully closed misconception.** E2 Q14 = 0
  (answered with reversible public-key encryption plus Diffie-Hellman — a
  confident wrong mechanism, worse than a blank) → OWASP reading → **E4 Q14b = 1**,
  with irreversibility named and hash-and-verify described. Residual polish only:
  say *salted* and *deliberately slow*, and name bcrypt/argon2.
- AuthN vs AuthZ with correct 401/403 (E3 Q13 = 1).
- Bearer tokens: header placement, post-auth issuance, HTTPS-or-stolen
  (E5 Q13 = 1).

### Unmeasured checklist items

**IDOR / broken object-level authorization** — named in the guide as the most
common real vulnerability, never asked · sessions vs tokens as
revocation-vs-scale, and the short-access-token + rotating-refresh-token
resolution · JWT payload is readable base64 · `alg: none` and RS256→HS256
confusion · the four OAuth2 roles · authorization code + PKCE, what PKCE and
`state` each defend · why implicit and password grants are deprecated · OIDC ID
tokens and never using one to call an API · salt vs work factor as separate
ideas · Argon2id/bcrypt/scrypt and memory-hardness · TLS gives three things,
mTLS adds client identity · the preflight OPTIONS request and
`Allow-Origin: *` + credentials · **CSRF: ambient credentials only, so a
Bearer API isn't vulnerable; SameSite / synchronizer token / double-submit**
(M3 Q13 pending) · table and column names can't be parameterized · XSS and
context-aware output encoding, CSP, HttpOnly · **a committed secret must be
rotated, not deleted** · the OWASP Top 10 as a list · SSRF and post-DNS
private-range blocking · never `ObjectInputStream` on untrusted input ·
per-account login rate limiting and identical timing.

---

## 14 — Messaging & queues

> **Understanding ledger:** `src/knowledge/understanding.md` §14 — score **L1**, measurement confidence **HIGH**. Reconciliation: agrees; both call the broker lifecycle the record's most persistent wrong model.

**Status: MEASURED.** Eight questions, and the source of the record's single
most persistent wrong model.

### Confirmed gaps

**GAP: broker message lifecycle — one systematic wrong model, three papers**
- Evidence: E3 Q15 = 0.5 (consumers down → answered "retry to consumers");
  E4 Q15 = 0.5 (DLQ described as messages that "couldn't be sent");
  E5 Q15 = 0.5 (consumers down for an hour → answered DLQ). The belief is
  consistent: *the broker retries and then DLQs regardless of consumers.*
  The correct model — consumers down means messages **wait durably**, and a DLQ
  is reached only after delivery plus N **processing** failures — never appeared.
- Severity: **HIGH** — three consecutive papers, and the guide marks both
  statements in bold as the topic's load-bearing facts.
- Type: unknown-unknown; the Q-Bank was built to find exactly this.
- Remediation: **CHAPTERED, unverified** — `fundamentals-primer-2.md` part 2 is
  dedicated to it. Retests: M1 Q15 (scored 0, blank — taken before study),
  M3 Q15 (poison message, still fresh).

**GAP: delivery semantics**
- Evidence: M1 Q15 = 0, blank.
- Severity: HIGH — at-most-once vs at-least-once vs exactly-once-effect is the
  vocabulary every messaging conversation runs on.
- Remediation: CHAPTERED — `primer-3.md` §4 (four-line taxonomy).

**GAP: idempotent consumer**
- Evidence: M2 Q15 = 0, blank — and primer-2 part 2 plus primer-3 §4 both named
  this paper as their retest.
- Severity: HIGH. Remediation: CHAPTERED, retest failed once.

**GAP: SQS vs Kafka**
- Evidence: M5 Q15 = 0, blank.
- Severity: MEDIUM (INFO-adjacent: the plan's Kafka track teaches this, and
  `tmp/gaps.md` sets messaging's reference level at L1–L2).

### Verified non-gaps / strengths

- Why a queue exists: delivery survival across consumer/network trouble plus
  asynchrony (E1 Q15 = 1).
- Producer / consumer / broker roles as vocabulary (E3 Q15, the scoring half).

### Unmeasured checklist items

Decoupling vs buffering vs asynchrony as three distinct reasons · a queue trades
throughput for latency plus eventual consistency · queue depth/lag rising and
messages dying at **retention expiry** · draining a backlog is a throughput
problem · both Kafka and SQS are pull-based · Kafka EOS covers Kafka-to-Kafka
only · the dedup-insert-in-the-same-transaction rule · naturally idempotent
operations over dedup tables · Redis dedup can't join a DB transaction · poison
messages and retryable-vs-not classification · **exponential backoff with
jitter, and why jitter is the part that matters** (M4 Q15 pending) · in-process
retry blocks the partition · alert on DLQ depth > 0 and a tested replay path ·
send-to-DLQ-and-confirm before acking · **ordering only per partition / FIFO
message group** (M3 Q16 pending) · hot keys and per-partition lag · a
single-partition topic caps consumers · handing messages to an executor destroys
ordering · **queue vs log: consuming deletes vs advancing an offset** ·
commands→queue, events→log · partition count as the scaling ceiling · adding
partitions breaks key ordering · `max.poll.interval.ms` eviction and rebalance
loops · retry topics · consumer lag as the health metric · `acks=all` +
`min.insync.replicas` · log compaction · **SQS visibility timeout and
concurrent duplicate processing when processing outruns it** · `maxReceiveCount` ·
long polling, batch 10, 256 KB claim-check · **dual write has no atomicity;
`@Transactional` doesn't cover the broker send** · transactional outbox · CDC /
Debezium · the inbox pattern · retry storms and 27× amplification · circuit
breakers, retry budgets, bulkheads, load shedding · scheduled jobs on N replicas
(M4 Q16 pending) · lock TTL expiring mid-work, fencing tokens · K8s CronJob is
at-least-once · backpressure and why a broker converts it into unbounded lag.

---

## 15 — Caching

> **Understanding ledger:** `src/knowledge/understanding.md` §15 — score **L2.5**, measurement confidence **HIGH**. Reconciliation: agrees; ledger absorbed this file's split — MEDIUM for the missing name, HIGH for the write path.

**Status: MEASURED.** Eight questions. **This is the candidate's single banked
strength** — the only topic that scored full marks at a tier above their
measured level.

### Confirmed gaps

**GAP: cache-aside as a named pattern**
- Evidence: M1 Q16 = 0, blank. Notable: the E2 Q15 TTL answer *described the
  cache-aside read path perfectly*. This is a vocabulary gap plus a genuine hole
  on the **write** path (delete-don't-update, and the read/write race).
- Severity: MEDIUM (vocabulary), HIGH for the write path — "delete the key,
  don't update it" is the guide's bolded correctness rule.
- Remediation: CHAPTERED — `primer-3.md` §1.

**GAP: stampede mitigations**
- Evidence: M2 Q16 = 0.5 — **thundering herd correctly named**, which is
  genuinely good, but the mitigations (TTL jitter, single-flight per-key lock,
  refresh-ahead) didn't follow.
- Severity: MEDIUM.

**GAP: Redis data structures**
- Evidence: E5 Q16 = 0.5 — "distributed caching" given, but the asked instance
  (two structures and their uses) was blank. Hash for session/object fields,
  sorted set for leaderboards and rate-limit windows, list, set, per-key TTL.
- Severity: MEDIUM.

**GAP: hit-ratio diagnosis**
- Evidence: E3 Q16 = 0.5 — definitions and verdict right; only one check named
  (size/evictions). Missing: TTL too short, key design mismatched to the access
  pattern, caching rarely-re-read data.
- Severity: MEDIUM.

### Verified non-gaps / strengths

- **Caffeine + Redis hybrid design (M5 Q16 = 1)** — full marks on a medium paper
  taken cold, with the read-heavy/in-process vs updates/central split, the
  near-cache invalidate-and-refill, **and an unprompted staleness caveat**. The
  best single answer in the entire record.
- Redis vs Postgres: in-memory vs disk (E1 Q16 = 1).
- TTL including the small-TTL-vs-stale trade-off (E2 Q15 = 1, above the bar).
- In-process vs external cache, with per-instance inconsistency at 4 instances
  spotted (E4 Q16 = 1).
- Easy-tier caching section: 2/2, 2/2, then consistently strong.

### Unmeasured checklist items

Hit-ratio non-linearity (90%→99% cuts DB load 10×) · a cache flush dropping you
to 0% and what that says about criticality · what not to cache · **delete-vs-update
on write and the reader-repopulates-stale race** · delayed double delete,
CDC invalidation · read-through vs write-through vs write-behind · never invent
write-behind for business data · **TTL jitter** · sliding vs absolute expiry ·
single-flight/coalescing · refresh-ahead · **serve-stale-on-error** ·
penetration vs avalanche · versioned key prefixes for atomic mass invalidation ·
multi-instance staleness flapping · `recordStats()` · **Redis pub/sub is
fire-and-forget, so a short L1 TTL is the actual guarantee** · Redis
single-threaded execution and `KEYS *` blocking everyone · ZSET/Set/List/Bitmap/
HyperLogLog/Streams selection · lazy + sampled expiry · Redis-as-queue
(`BLPOP` at-most-once) · **persistence is lossy by default; async replication
can lose writes on failover** · `maxmemory-policy` defaults to `noeviction`
(writes fail) · `allkeys-lru` vs LFU and scan pollution · eviction ≠ expiry ·
**every input that changes the output belongs in the key** (omitting user/role
serves one user's data to another) · `SCAN` not `KEYS`, `UNLINK` not `DEL` ·
negative caching with a sentinel · Bloom filters · cold start as an outage,
warming behind a failing readiness probe but out of liveness · HTTP caching
tier, `stale-while-revalidate`, `public` on authenticated responses.

---

## 16 — Testing

> **Understanding ledger:** `src/knowledge/understanding.md` §16 — score **L1**, measurement confidence **HIGH**. Reconciliation: ledger lowered L1.5 → L1 in its pass 2 citing this section's CRITICAL on the test-double taxonomy.

**Status: PARTIALLY MEASURED.** Seven questions; easy tier fine, medium tier
uniformly blank.

### Confirmed gaps

**GAP: the test-double taxonomy**
- Evidence: M1 Q17 = 0, blank (mock vs stub vs fake) — the candidate explicitly
  requested a chapter.
- Severity: **CRITICAL** by the letter of the scoring rules (L0 in testing, a
  daily-use area); **HIGH** in practice, since easy-tier mock questions scored 1.
- Remediation: CHAPTERED — `primer-3.md` §2, including the what-to-mock decision
  rule. Unverified.

**GAP: `Clock` injection / deterministic time**
- Evidence: M1 Q18 = 0, blank.
- Severity: MEDIUM. Remediation: CHAPTERED — `primer-3.md` §5. Retest: **M4 Q17**
  needs it directly (a JUnit 5 + Mockito writing exercise).

**GAP: flaky tests**
- Evidence: M2 Q17 = 0, blank; the candidate requested a test-types guide, now
  in `tmp/primers/study-plan.md`.
- Severity: MEDIUM. Remediation: CHAPTERED.

**GAP: integration-test definition inverted**
- Evidence: E1 Q17 = 0.5 — integration tests described as using "actual
  dependencies in mocked format," which is the definition of a unit test with
  doubles. Integration tests use **real** collaborators.
- Severity: MEDIUM. Never retested.

**GAP: Mockito specifics**
- Evidence: M5 Q17 = 0.5 — `doReturn`-style stubbing for void methods correct;
  `ArgumentCaptor` and why verify-everything welds tests to implementation both
  missing.
- Severity: MEDIUM. Retest: M4 Q17.

**GAP: `assertEquals` argument order**
- Evidence: E4 Q17 = 0.5 — treated as convention; it isn't, because failure
  messages print "expected X but was Y" and a swap produces lying diagnostics.
- Severity: INFO.

**Attribution note (reconciliation).** `understanding.md` §16's commentary cites the
CI-defined-as-CD miss (E4 Q18) as testing evidence. **It is filed here under §20**
(observability and operations), where deploy-pipeline literacy lives — and that
file's own §20 accepts the reassignment. Testing's probe count is therefore 7, not 8;
neither file's score changes, but do not count E4 Q18 twice.

### Verified non-gaps / strengths

- Mocks: behaviour-mimicking, conditional returns, a sensible DB example
  (E1 Q18 = 1).
- Reading a test and naming given-when-then (E3 Q17 = 1).
- Unit-test definition and the speed rationale (E1 Q17, the scoring half).

### Unmeasured checklist items

Placing unit/slice/integration/contract/E2E/smoke/acceptance on a ladder ·
justifying the pyramid and knowing the testing-trophy counter-argument ·
mock boundaries you own, wrap third-party clients, never mock pure logic ·
prefer a fake for stateful collaborators · `when(mock.foo())` really invokes the
method, so spies need `doReturn ... when` · argument matchers are all-or-nothing
and need `eq()` · `@MockBean` changes the context cache key and multiplies
startups · JUnit 5 new-instance-per-method · `@ParameterizedTest` with
`@CsvSource`/`@MethodSource` · `assertThrows` over try/fail/catch ·
`isEqualByComparingTo` for BigDecimal · **H2 gives false confidence;
Testcontainers for anything touching SQL** (M3 Q17 pending) · static container +
context caching · **choosing the right slice** (`@WebMvcTest`, `@DataJpaTest`,
`@JsonTest`, `@RestClientTest`, `@SpringBootTest`) · `@DirtiesContext` destroying
context caching · `@DataJpaTest` rolls back, so flush explicitly · Awaitility
`untilAsserted` over `Thread.sleep` · behaviour-describing test names ·
**coverage measures execution, not assertion; mutation testing** ·
characterization tests before refactoring · consumer-driven contract testing and
`can-i-deploy`. **Ad-hoc paper 3 covers a slice of this.**

---

## 17 — Git craft (and debugging methodology)

> **Understanding ledger:** `src/knowledge/understanding.md` §17 — score **L2**, measurement confidence **HIGH**. Reconciliation: agrees; ledger notes its L2 describes Git alone, since debugging methodology is unasked.

**Status: PARTIALLY MEASURED.** Seven questions, all on the everyday-commands
layer. The guide's recovery, archaeology, review, and debugging halves are
untouched — and the one recovery question asked came back blank.

### Confirmed gaps

**GAP: recovery and history tools**
- Evidence: M5 Q18 = 0, blank — `revert` vs `reset` vs `checkout`. Note E2 Q17
  scored 1 on commit/push/fetch/pull: **the basics are fine and the recovery
  layer is absent**, which is exactly the split `tmp/gaps.md` §3.1 predicted
  ("git beyond basics").
- Severity: **HIGH** — `revert` for pushed commits vs `reset` for local history
  is a [CORE] item, and `reset --hard` without knowing reflog is how work gets
  destroyed.
- Remediation: OPEN. Retest queued: **M4 Q18** (`git reset --hard HEAD~3`
  recovery) — reflog.

**GAP: merge-conflict resolution steps**
- Evidence: E2 Q18 = 0.5 — when conflicts are raised: right. The mechanical
  steps (edit the file, remove the `<<<<<<<` markers, `git add`, then
  `commit`/`--continue`) were replaced by the *decision* (ours/theirs/both).
- Severity: MEDIUM.

**GAP: rebase safety rules**
- Evidence: M2 Q18 = 0.5 — merge-vs-rebase direction roughly right; the golden
  rule (never rebase shared history) and `--force-with-lease` vs `--force` both
  missing.
- Severity: MEDIUM. Remediation: OPEN.

**GAP: commit-message craft**
- Evidence: E5 Q17 = 0.5 — the example subject named the bug (good), but the
  model was "detailed verbose" rather than concise subject + body carrying the
  **why**.
- Severity: INFO.

### Verified non-gaps / strengths

- commit / push / fetch / pull all four correct, including a correct hedge that
  `git pull --rebase` exists (E2 Q17 = 1).
- Branching and PRs as a review gate keeping `main` releasable (E3 Q18 = 1).
- **Code review: three distinct items with a sane priority order —
  correctness → optimization → style last (E5 Q18 = 1).** This directly answers
  `tmp/gaps.md` §3.1's "code review skills" concern: it is not a gap.

### Unmeasured checklist items

Git stores snapshots; rewriting a commit rewrites all descendants · the four
areas · a branch as a movable pointer, `HEAD` · `git add -p` · `git diff` vs
`--staged` vs `main...HEAD` · fetch is always safe · `main` deployable at every
commit · short-lived branches and feature flags · squash-merge as the default ·
**"ours"/"theirs" swapping meaning during a rebase** · run tests before
`--continue` · `rerere` · `reset --soft` vs `--mixed` vs `--hard` ·
`git clean -fd` destroys irrecoverably · `git restore`/`git switch` · reverting a
merge with `-m 1` and the re-merge block · **reflog** (M4 Q18 pending) ·
`git fsck --lost-found` · **`git bisect`, `bisect run`, exit codes
0/1/125** (M3 Q18 pending) · `git stash -u` · `cherry-pick -x` for backports ·
`rebase -i` verbs and `--autosquash`/`--fixup` · the 50/72 commit format and the
`git blame`-in-18-months test · automate style entirely · **defect detection
collapses past 200–400 changed lines** · labelling comments
`blocking:`/`suggestion:`/`nit:` · two-round-trips-then-a-call · **the debugging
loop: observe → reproduce → falsifiable hypothesis → predict → one variable →
fix the cause → regression test** · binary-searching the problem space ·
**"what changed?" first** · correlation IDs · MDC is ThreadLocal, clear in a
finally, doesn't cross async · patterns behind "random" 1% bugs · a failure rate
matching 1/(instance count) · amplifying a rare failure · verifying a rare-bug
fix statistically · `.gitignore` only affects untracked files, `git rm --cached` ·
**deleting a secret in a later commit doesn't remove it from history — rotate
first, then `git filter-repo`** · pre-commit secret scanning.
**Ad-hoc paper 3 covers this.**

---

## 18 — Cloud & AWS

> **Understanding ledger:** `src/knowledge/understanding.md` §18 — score **L1**, measurement confidence **HIGH**. Reconciliation: ledger lowered L1.5 → L1 in its pass 2, adopting the "real level ≈ L1, not L2" conclusion recorded here.

**Status: PARTIALLY MEASURED.** Nine questions, all at the name-the-service or
name-the-concept level. Every question that reached operational depth scored
≤ 0.5.

### Confirmed gaps

**GAP: operational depth behind the concepts**
- Evidence: the E1↔E2 swing is the cleanest illustration in the record —
  E1 sampled strengths (EC2/S3/RDS one-liners, 1.5/2) and E2 sampled operations
  (12-factor config Q19 = 0.5, load-balancer capabilities Q20 = 0) for 0.5/2.
  E2 Q20 asked for two things a load balancer does *beyond* spreading traffic
  and got spreading restated; health checks, connection draining for
  zero-downtime deploys, and TLS termination were the marks.
- Severity: **HIGH**. `tmp/gaps.md` §9 records the conclusion: **real level ≈ L1,
  not L2** — below the L2 reference for this topic.
- Remediation: OPEN. The plan's Day 22–23 AWS on-ramp must not be compressed.

**GAP: IAM roles vs access keys**
- Evidence: M1 Q19 = 0.5 — granularity offered as the differentiator; the
  mechanical answer (roles vend **temporary, auto-rotated** credentials from
  IMDS / the ECS endpoint / STS, so there is no static secret to leak, rotate,
  or commit) missing.
- Severity: MEDIUM.

**GAP: secrets management vs configuration**
- Evidence: M2 Q20 = 0.5 — env vars for config correct; secrets coming from a
  secrets manager injected at runtime, and never appearing in git or image
  layers, missing (and tying to the M2 Q19 Dockerfile finding).
- Severity: HIGH ([CORE] secrets hygiene; the scoring rules name it).

**GAP: health-check consumers**
- Evidence: E4 Q20 = 0.5 — definition fine; **who calls it** blank (load
  balancer stops routing, orchestrator restarts and gates deploys, monitoring
  alerts).
- Severity: MEDIUM. Partially addressed by M1 Q20 (0.5, see topic 19).

**GAP: environment-parity bug classes**
- Evidence: E5 Q20 = 0.5 — definitions and the staging rationale fine; the asked
  bug class blank (works-on-H2-fails-on-Postgres, missing prod config/secret,
  pool exhaustion invisible at staging's load).
- Severity: MEDIUM.

### Verified non-gaps / strengths

- EC2 / S3 / RDS purposes (E1 Q19 = 1).
- Region vs AZ including the HA rationale (E3 Q19 = 1).
- Vertical vs horizontal scaling, LB → horizontal, **plus an above-bar
  observation about leader election / coordination limits** (E5 Q19 = 1) — that
  is the scheduled-job double-run problem, recognized unprompted.
- Container logs → CloudWatch, and logs die with the container (E4 Q19 = 1).
  **Dual-credited:** this answer is also listed under §20, since it spans the cloud
  destination and the logging principle. `understanding.md` §19 makes the same call
  and explicitly strips it from topic 19. It is one answer, counted once in the
  99-answer easy-tier total — do not add §18 and §20 probe counts together.

### Unmeasured checklist items

AZ as an independent failure domain, three AZs meaning 33% not 50% ·
cross-region replication is async so failover loses writes · multi-region needs
an RTO/RPO justification · AZ IDs vs names · `t`-instance burst credits ·
instance store does not persist · Spot and Savings Plans · **IMDSv2 and the
SSRF→credential-theft path** · **S3 is object storage, not a filesystem**;
strong read-after-write since 2020; versioning because durability is not backup ·
lifecycle rules and aborting incomplete multipart uploads · **presigned URLs** ·
buckets private by default, CloudFront + OAC · RDS connection-count ownership
(`pods × poolSize < max_connections`) and **RDS Proxy** · Aurora's 6 copies /
3 AZs · **Lambda cold starts for the JVM**, SnapStart, the 15-min/10 GB/6 MB
limits, globals persisting · **SNS → multiple SQS fan-out**, EventBridge ·
ECS task definition + service, Fargate · **ALB (L7) vs NLB (L4)** ·
cross-zone load balancing · Route 53 Alias · CloudWatch log groups never expire
by default · every unique metric dimension is separately billed · policy
evaluation order (explicit Deny > Allow > implicit deny) · SCPs and
resource-based policies · least privilege from `AccessDenied` / Access Analyzer ·
MFA, no root, CloudTrail, account-per-environment · a subnet is in exactly one
AZ · public vs private subnet, NAT egress · **security groups stateful and
allow-only, NACLs stateless with deny; the ephemeral-port NACL rule** ·
SGs referencing SGs · **S3/DynamoDB Gateway Endpoints are free** · VPC Flow
Logs · managed-service trade-off · Parameter Store vs Secrets Manager ·
inject secrets by ARN · a secret cached forever defeats rotation ·
statelessness breakers (local sessions/files/caches, per-instance jobs,
in-memory rate limiters multiplying by N, WebSocket registries) · sticky
sessions as a crutch · scale out fast, in slow; warm-up as a scaling parameter ·
don't scale the non-bottleneck tier · **cross-AZ transfer charged both ways**,
NAT per-GB, forgotten EBS/EIP/ALB · **Multi-AZ (availability) vs read replica
(read scaling)** · replica lag and read-after-write · RDS failover is DNS-based
and the JVM DNS cache pins the old primary.

---

## 19 — Docker & Kubernetes

> **Understanding ledger:** `src/knowledge/understanding.md` §19 — score **L1**, measurement confidence **LOW**. Reconciliation: agrees; ledger reassigned E4 Q19 out of this topic, leaving 4 probes, none above 0.5.

**Status: UNMEASURED.** Four questions in nine papers, none scoring above 0.5.

### Confirmed gaps

**GAP: container fundamentals precision**
- Evidence: E1 Q20 = 0.5 — image-as-template vs container-as-instance right, but
  an image was called "an OS's copy." Containers **share the host kernel** —
  that is the entire difference from a VM — and an image is an immutable stack
  of filesystem layers.
- Severity: MEDIUM.

**GAP: Dockerfile review**
- Evidence: M2 Q19 = 0.5 — the hardcoded password was spotted (good), but four
  of five defects were missed: unpinned `openjdk:latest`, `COPY .` before the
  build busting the dependency cache, running as root, single-stage shipping the
  JDK and sources. One claim was also wrong (`target/` "won't be present" —
  `mvnw package` creates it).
- Severity: **HIGH** — image hygiene is directly asked, and it compounds with
  the secrets gap in topic 18. Retest queued: **M4 Q19** (layers and cache
  invalidation).

**GAP: OOMKilled vs OutOfMemoryError**
- Evidence: M5 Q19 = 0, blank.
- Severity: HIGH — exit 137 with no stack trace and a JVM heap OOM have
  **opposite fixes**, and this is the defining container-JVM interview question.
- Remediation: OPEN.

**GAP: probe consequences**
- Evidence: M1 Q20 = 0.5 — liveness and readiness defined correctly; the actors
  (orchestrator restarts vs load balancer routes) and the failure story
  (liveness wired to a DB dependency restart-loops healthy pods during an
  outage, preventing recovery) both missing.
- Severity: MEDIUM.

### Verified non-gaps / strengths

None at full marks. The definitional layer is present in every answer; nothing
above it has been demonstrated.

### Unmeasured checklist items

Namespaces (what you see) vs cgroups (what you use) · shared kernel means no
cross-OS containers and weaker isolation than a VM · a non-cgroup-aware process
sees the host's CPU/memory · containers are about reproducible artefacts, not
efficiency · **invalidating one layer invalidates every layer after it; copy
dependency manifests before source** (M4 Q19 pending) · `apt-get update &&
install` in one `RUN` · deleting a file in a later layer doesn't remove the
bytes · BuildKit cache mounts and `--mount=type=secret` · pin base tags ·
**multi-stage: build with JDK, ship JRE** · never secrets in `ENV`/`ARG`
(`docker history`) · run as non-root · `.dockerignore` · **exec-form ENTRYPOINT,
because shell form makes `/bin/sh` PID 1 and SIGTERM is never forwarded** ·
PID 1 doesn't reap zombies · `docker exec` vs a crash-looping container ·
named volumes vs the writable layer · Compose DNS and `depends_on` ordering ≠
readiness · **never deploy `:latest`; tag with version + SHA; rollback by tag** ·
Pod/Deployment/ReplicaSet/StatefulSet/DaemonSet · Service vs Ingress ·
**Kubernetes Secrets are base64, not encrypted** · declarative reconciliation ·
**liveness restarts / readiness deregisters / startup gates**, and never put
dependency checks in liveness · `startupProbe` for slow JVM boots · Spring Boot's
liveness/readiness actuator endpoints · **requests = scheduling, limits =
runtime ceiling; CPU over-limit throttles, memory over-limit OOMKills** ·
memory limit == request for Guaranteed QoS · CFS throttling invisible in CPU
graphs · the cgroup limit covers non-heap memory too · `MaxRAMPercentage=70` ·
`availableProcessors()` following the CPU limit · `maxUnavailable: 0` +
`maxSurge: 1` · **endpoint removal and SIGTERM happen concurrently — hence the
`preStop` sleep** · PodDisruptionBudgets · HPA measuring against requests and
needing Cluster Autoscaler · `kubectl describe pod` Events first ·
`kubectl logs --previous`, `port-forward` · check your context before a
destructive command · ECS+Fargate as the simpler default.
**Ad-hoc paper 2 covers this.**

---

## 20 — Observability & operations

> **Understanding ledger:** `src/knowledge/understanding.md` §20 — score **—** (no level placed), measurement confidence **NONE**. Reconciliation: agrees; ledger accepts this file's reassignment of E3 Q20 and E4 Q18 here and still records no score.

**Status: UNMEASURED.** Four questions, and the only one at design level came
back blank. The largest guide with the least measurement.

### Confirmed gaps

**GAP: alert design**
- Evidence: M5 Q20 = 0, blank.
- Severity: MEDIUM (expected pre-plan), but the guide's content — alert on
  symptoms not causes, page/ticket/dashboard classification, burn-rate
  alerting — is standard senior-screen material.
- Remediation: OPEN.

**GAP: CI conflated with CD**
- Evidence: E4 Q18 = 0.5 — CI defined as deployment. CI is automatically
  **building and validating** every change; it says nothing about deploying.
  The standard checks (build, unit + integration tests, lint/static analysis,
  security scan, coverage gate) were only partially given.
- Severity: **HIGH** — the scoring rules name deploy-pipeline literacy zeros as
  HIGH, and `tmp/gaps.md` §3.1 already flags it. "What does your CI run?" is a
  standard screen.
- Remediation: OPEN (a one-liner fix, but unverified).

### Verified non-gaps / strengths

- Rollback: versioned artifacts/tags plus a rollback step in the pipeline
  (E3 Q20 = 1).
- Container logs go to CloudWatch and die with the container (E4 Q19 = 1;
  **dual-credited with §18** — one answer, not two) — the
  underlying principle (apps write to stdout/stderr, the platform ships them)
  was implicit rather than stated.

### Unmeasured checklist items

**Logs vs metrics vs traces and which question each answers** (M3 Q20 pending) ·
the metric-alerts → trace-localises → log-explains workflow · monitoring vs
observability · structured JSON logging with variables in fields · always log
version/git SHA and trace id · never `log.error(e.getMessage())` ·
**never log secrets or PII** · correlation ID generated at the edge, stored in
**MDC**, returned in a header · `MDC.clear()` in a finally · MDC doesn't cross
`@Async` without a `TaskDecorator` · level discipline · don't log-and-rethrow ·
runtime-adjustable log levels · **RED and USE** · instrument RED per endpoint and
per dependency · **averages lie; p50/p95/p99 and the widening p50→p99 gap** ·
**you cannot average percentiles** · tail latency amplification with fan-out ·
histograms vs summaries · **the cardinality trap: never put IDs in metric
labels** · counter vs gauge vs histogram · **business metrics catching what
infra metrics miss** · a trace as a tree of spans · OpenTelemetry and the Java
agent · W3C `traceparent` propagation · **tail-based sampling** ·
deep checks must never drive restarts · restrict `/actuator/env` and
`/heapdump` · exclude health checks from access logs · **CI vs Continuous
Delivery vs Continuous Deployment as three distinct things** ·
**build the artefact once and promote it** · fail fast, red build is
stop-the-line · **recreate / rolling / blue-green / canary / feature flags** ·
rolling and canary run both versions at once, so every change must be backward
compatible · rollback enablers and a *practised* procedure · the zero-downtime
chain · **expand/contract schema migration; never ship a schema change and its
dependent code in the same deploy** · incident command and mitigate-before-
diagnose · "what changed?" · fixed-cadence communication · **blameless
postmortems** and action items with owners · **alert on symptoms, not causes** ·
page/ticket/dashboard classification · alert fatigue · **burn-rate alerting** ·
alerting on absence and saturation · dashboards with deploy annotations ·
runbooks linked from alerts · sustainable on-call · **SLI → SLO → SLA and the
error budget as a resource to spend** · 99.9% = 43 min/month.
**Ad-hoc paper 2 covers this.**

---

## Cross-topic findings (not owned by any single guide)

These recur across topics and cost more marks than several individual gaps.

**1. Answer-the-asked-instance failure — ~10 occurrences, ~2 marks per paper.**
E1 Q9/Q10, E2 Q9/Q10/Q20, E3 Q7, E5 Q12/Q16/Q20, and in code form at the code
session (E4 Q2's missing complexity statement). The pattern: the concept is
known, the specific clause the question asked is never answered — `SELECT *`
for "names and salaries," "beyond spreading traffic" answered with spreading,
"what starts" left blank. **Severity: HIGH** by cost. This is the cheapest score
lever available. Remediation: standing pre-submit ritual — re-read the question
and tick every clause; also scan for unfilled placeholders (E3 Q6 was submitted
containing `***ADD ANSWER HERE***`).

**2. Confidence miscalibration.** Self-flagged answers (E5 Q4 generics, E5 Q19
scaling) scored full marks; confidently given ones (E5 Q5 volatile safety,
E5 Q15 SQS→DLQ) were wrong. Severity: MEDIUM. Interview implication: hedge less
on knowledge, verify compound claims before asserting them.

**3. Study execution is now the bottleneck, not measurement.** The one completed
loop — concurrency, 0.5 → 0.5 → primer → 1.5 → 2.0 — is the only intervention in
the record with a measured effect. Every measurement taken without study
flatlined: M5 3.5, M1 6.5, M2 3.0. At M2, four of the misses (CORS, idempotent
consumer, deadlock explanation, stampede) had **already-written chapters that
named that exact paper as their retest**. Severity: **CRITICAL as a process
finding** — `tmp/gaps.md` §9 draws the right conclusion for the plan rewrite:
the 28-week plan must enforce study-before-measure gates, not merely provide
material.

**4. Written-but-unverified remediation inventory.** Five artifacts exist and
only one (the concurrency primer) has a verified effect:
`tmp/primers/concurrency-primer.md` (verified twice, one regression),
`tmp/primers/fundamentals-primer-2.md` (ACID partial pass, broker/CORS/volatile
unverified or failed), `tmp/primers/primer-3.md` (unverified),
`tmp/primers/quick-notes.md` (unverified), `tmp/primers/study-plan.md` (not run).

---

## Severity roll-up

| Severity | Count | Items |
|---|---|---|
| **CRITICAL** | 4 | Spring proxy model (07) · JPA sharp edges LazyInit/N+1 (08) · JPA entity states (08) · test-double taxonomy (16) — plus study-execution as a process finding |
| **HIGH** | 27 | Code write-fluency (01) · modern Java post-8 (04) · volatile-vs-atomicity repeat (05) · compound actions on concurrent maps (05) · thread pools (05) · OOM taxonomy (06) · fetch-type defaults (08) · ACID (09) · SQL word-problem recognition (09) · index mechanics (09) · transactions/lost update (09) · TCP+TLS handshakes (10) · Linux tooling (11) · CORS repeat-miss (13) · JWT validation (13) · prepared statements (13) · broker lifecycle (14) · delivery semantics (14) · idempotent consumer (14) · cache-aside write path (15) · git recovery tools (17) · cloud operational depth (18) · secrets management (18) · Dockerfile hygiene (19) · OOMKilled vs OOM (19) · CI vs CD (20) · asked-instance pattern (cross-topic) |
| **MEDIUM** | ~30 | Listed in place under each topic |
| **INFO** | 3 | `assertEquals` order (16) · commit-message craft (17) · OAuth2 flows, plan-taught (13) |

## Topic status roll-up

| Status | Count | Topics |
|---|---|---|
| MEASURED | 8 | 02 collections · 03 java-core · 05 concurrency · 09 sql · 10 networking (breadth only) · 13 security · 14 messaging · 15 caching |
| PARTIALLY MEASURED | 6 | 01 dsa · 07 spring-core · 08 spring-data-jpa · 16 testing · 17 git-craft · 18 cloud-aws |
| UNMEASURED | 6 | 04 modern-java · 06 jvm-internals · 11 os-linux · 12 api-design (specifics only) · 19 docker-k8s · 20 observability |

---

## Ad-hoc instruments created for the unmeasured territory

Three papers were written to cover the six UNMEASURED topics plus the
pattern-recognition half of 01. They sit at the **easy→medium boundary** —
deliberately below M3/M4 so they measure breadth without spending the two
remaining fresh medium instruments.

| Paper | Covers | Questions | Files |
|---|---|---|---|
| Ad-hoc 1 | 04 modern Java · 06 JVM internals + diagnostics · 01 DSA pattern recognition · 02 collections internals | 18 | `tmp/ad-hoc-paper-1.md`, `tmp/ad-hoc-paper-1-key.md` |
| Ad-hoc 2 | 11 OS/Linux tooling · 19 Docker/Kubernetes · 20 observability, deploys, incidents | 20 | `tmp/ad-hoc-paper-2.md`, `tmp/ad-hoc-paper-2-key.md` |
| Ad-hoc 3 | 12 API-design specifics · 17 git craft + debugging methodology · 16 testing breadth · 18 AWS breadth | 18 | `tmp/ad-hoc-paper-3.md`, `tmp/ad-hoc-paper-3-key.md` |

**Sequencing note.** These do not displace `tmp/primers/study-plan.md`. Their
purpose is breadth measurement on territory no instrument has touched, so the
study program can be sized against evidence rather than assumption. `[CODE]`
questions follow the existing deferred-batch policy (current deferred list:
E2 Q2, E4 Q2, E5 Q10, M5 Q1, M1 Q9, M2 Q2).

---

## Coverage audit (pass 2)

Every topic 01–20 was checked for at least one of: (a) measured evidence cited,
(b) an unmeasured-concepts list, (c) a named pending instrument.

**Result: 20 of 20 pass, with no topic relying on a single criterion.** All twenty
carry both cited question-level evidence and an `Unmeasured checklist items` list;
the six UNMEASURED topics plus 01 and 02 additionally name a pending ad-hoc paper,
and thirteen topics name a specific pending M3/M4 question as a queued retest. No
topic required a fix.

The audit did surface three attribution overlaps rather than coverage holes, each
now annotated in place so the two files cannot be read as contradicting: E4 Q19
(container logs) is dual-credited to §18 and §20 and must be counted once; E4 Q18
(CI vs CD) is filed under §20, not §16; and topic 06 deliberately carries an
UNMEASURED *status* here alongside an L1 *level* in the ledger, because those
measure different things.

---

## Backlog: unmeasured, no instrument yet

Twenty-three clusters that no paper has probed and that **none** of the three
ad-hoc papers, M3, or M4 will reach. Listed rather than turned into papers, per
the standing instruction — the ad-hoc set is already three untaken instruments and
adding a fourth would compound the study-execution problem that is currently the
binding constraint.

Nothing here is a measured gap. These are blind spots: the honest statement is that
the profile is unknown in these areas, not weak in them.

| # | Cluster | Topic | Why it matters | Why no instrument reaches it |
|---|---|---|---|---|
| 1 | DP, greedy, graphs, tries, union-find | 01 | The half of the DSA guide that carries hard interview problems | Ad-hoc 1 Q1 samples window / monotonic stack / two-pointer / binary-search-on-answer only |
| 2 | TreeMap & NavigableMap navigation, EnumSet, Comparator chains, TimSort contract | 02 | `floor`/`ceiling`/`subMap` is the reason to pick TreeMap; comparator-by-subtraction throws | Ad-hoc 1 Q5–Q6 cover LinkedHashMap-as-LRU and CME only |
| 3 | Java core residue: `java.time` model, try-with-resources & suppressed exceptions, `return` in `finally`, enum singleton, inner-class leaks, lambda capture | 03 | `LocalDateTime` is not an instant, and `Duration` vs `Period` across DST is a classic | BigDecimal and `Optional` are deliberately reserved to M4 Q3/Q4; the rest was never assigned |
| 4 | Structured concurrency; parallel streams and the common pool | 04 | The Java 21 answer to `CompletableFuture.allOf`; blocking in a parallel stream starves the JVM | Ad-hoc 1 Q12 covers virtual threads; these are the adjacent items |
| 5 | Synchronizers: CountDownLatch, CyclicBarrier, Semaphore, `wait()` in a while loop, `ReentrantLock` features | 05 | Standard follow-ups once `synchronized` is discussed | M4 Q5/Q6 cover `counter++` fixes and ThreadLocal; coordination primitives are unassigned |
| 6 | Memory-model formalism: the happens-before edges, safe publication, ABA / `AtomicStampedReference` | 05 | The *why* under volatile and CAS — the layer above the repaired basics | No paper asks it; the primer teaches volatile without the formal edges |
| 7 | JIT tiers, deoptimization and warmup; JMH; JFR; ZGC; classloader leaks | 06 | "Why is the first minute after deploy slow" is a real screen | M3 Q6 covers generational GC; ad-hoc 1 covers OOM taxonomy and the CPU/leak workflows |
| 8 | **Transaction propagation and rollback rules**: `REQUIRES_NEW` vs `NESTED`, checked exceptions commit by default, `UnexpectedRollbackException`, `readOnly` | 07 | The highest-value item in this backlog. It sits directly on the CRITICAL proxy-model gap and is asked constantly at this YOE | M1 Q8 probed self-invocation and scored 0; M3/M4 retest the **proxy mechanism**, not propagation semantics |
| 9 | Spring web/async/config infrastructure: DispatcherServlet flow, filter vs interceptor vs aspect, `@Async` exception swallowing, `@TransactionalEventListener`, auto-configuration mechanics, `@ConfigurationProperties`, property precedence | 07 | Framework-mechanism questions that follow "how does Boot start up" | M3 Q7 asks the field-injection argument only |
| 10 | JPA locking, batching and schema tooling: `@Version` optimistic locking, `PESSIMISTIC_WRITE`, `flush`+`clear` batching, Flyway with `ddl-auto=validate`, `@Modifying`, `open-in-view` | 08 | Optimistic locking is the standard concurrency answer for a CRUD service | M4 Q7/Q8 cover the L1 cache and `merge`; the write-side operational layer is unassigned |
| 11 | Database operations: Hikari pool sizing and pool deadlock, MVCC and VACUUM bloat, replication lag, expand/contract migration, `CREATE INDEX CONCURRENTLY`, `FOR UPDATE SKIP LOCKED` | 09 | Pool sizing is the most common production database incident at this YOE | M3 Q10 and M4 Q9/Q10 cover anomalies, windows and UPSERT — all query-level, not operational |
| 12 | Transport and socket lifecycle: HTTP/3 and QUIC, L4 vs L7 balancing, TLS termination modes, CDN cache keys, `X-Forwarded-For`, `TIME_WAIT` arithmetic, `CLOSE_WAIT` as an fd leak | 10 | M2 Q11 measured the port-exhaustion miss and **no retest was ever queued** | M3 Q11 stops at HTTP/1.1 vs HTTP/2 |
| 13 | **IDOR / broken object-level authorization** | 13 | `13-web-security.md` names it the most common real vulnerability; it has never been asked at any tier | Not in any paper, easy through hard, nor in ad-hoc 3 |
| 14 | Security breadth: OWASP Top 10 as a list, XSS and context-aware output encoding, SSRF with post-DNS blocking, insecure deserialization, OAuth2/OIDC flows and PKCE | 13 | Three of these are standard screens; OAuth2 was blank at M5 Q14 | M3 Q13 covers CSRF, M4 Q13/Q14 cover session-vs-JWT and prepared statements |
| 15 | **Transactional outbox, CDC/Debezium, dual-write atomicity** | 14 | `@Transactional` does not cover the broker send — the single most-asked distributed-data question in a Spring/Kafka shop | No instrument at any tier touches it |
| 16 | Kafka partition and consumer-group mechanics: partitions as the scaling ceiling, rebalance and `max.poll.interval.ms`, consumer lag, log compaction, `acks=all` | 14 | The delivery-semantics gap is already HIGH; this is the layer it rests on | M3 Q16 asks about global ordering only |
| 17 | Resilience: retry storms and amplification, circuit breakers, bulkheads, retry budgets, load shedding, backpressure | 14 | Directly relevant to the plan's Project 2 hardening pass | M4 Q15 covers backoff and jitter; the surrounding patterns are unassigned |
| 18 | Redis operational model and cache key design: persistence lossiness, `maxmemory-policy` defaults, single-threaded `KEYS`, data-structure selection, every-input-in-the-key, versioned prefixes, negative caching, cold-start warming | 15 | The one place the banked strength (15) has an unlit corner — and omitting user/role from a key serves one user's data to another | E5 Q16 measured the structure-inventory miss; nothing retests it |
| 19 | Consumer-driven contract testing (Pact, provider verification, `can-i-deploy`) | 16 | The one testing layer with no coverage at all | M3 Q17 and ad-hoc 3 stop at Testcontainers, slices and mutation testing |
| 20 | Git history-editing craft: interactive rebase verbs, `--autosquash`/`--fixup`, `cherry-pick -x`, `rerere`; PR sizing and review-comment labelling | 17 | Take-home and PR-hygiene signal; the 200–400-line review-collapse threshold is a good talking point | Ad-hoc 3 covers reflog, revert/reset, bisect, force-with-lease and secret purging |
| 21 | AWS service and platform mechanics: S3 object-store semantics and presigned URLs, Lambda cold starts and limits, SNS→SQS fan-out, EventBridge, ECS/Fargate, ALB vs NLB, Multi-AZ vs read replica, cost drivers, IaC | 18 | Topic 18 is measured at name-the-service level; this is everything one layer down | Ad-hoc 3 Q17/Q18 cover IAM and VPC/SG/NACL only; M4 Q11 covers DNS failover |
| 22 | Kubernetes object model and scaling: Pod/Deployment/Service/Ingress/ConfigMap/Secret, HPA against requests, PodDisruptionBudgets, Secrets-are-base64 | 19 | No object-model question has been asked at any tier | Ad-hoc 2 covers probes, requests/limits, CrashLoopBackOff and rolling updates — the runtime layer, not the API |
| 23 | Observability instrumentation and release practice: Micrometer/Prometheus counter-gauge-histogram, OTel propagation and tail-based sampling, blue-green/canary/feature flags, expand/contract deploys, incident command and blameless postmortems | 20 | The CI-vs-CD miss (HIGH) sits in this territory | Ad-hoc 2 Q17–Q20 cover pillars, percentiles, MDC and alerting; M4 Q20 covers the zero-downtime lifecycle |

**How to use this list.** Do not build instruments for it now. Three items are worth
promoting the moment the current programme clears, because each is both
high-frequency and adjacent to an already-CRITICAL or HIGH gap: **#8 transaction
propagation** (sits on the proxy model), **#15 outbox/CDC** (sits on the broker
lifecycle), and **#13 IDOR** (a named guide priority with zero coverage anywhere).
The remaining twenty are genuine breadth and belong to the 28-week plan's normal
teaching path, not to diagnostics.