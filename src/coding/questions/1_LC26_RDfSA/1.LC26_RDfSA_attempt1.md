- I can see the array is sorted already, the question asks the elements to be removed in-place hence space comp must be O(1) I cant use another array
- The first obvious choice that's coming to my mind is loop through the array once, and along the way track current and previous current starts at 1st index and prev starts at 0th index
- We compare if c == p, if yes we mark c as replacable r, but now we'll have to track whether the r index is already replaced.
- I'm now thinking to compare if c>p, increment r for this case.
- After incrementing r make a[r]=a[c]

I will write a pseudocode here:
```
int p=0,c=1,u=-1,s=0;
while(c<arr.length) {
    if (arr[c]>arr[p]) {
     if (s!=-1) {
        arr[s+1]=arr[c]
     }
     s++;
    }
    c++; p++;
}
```
- I thought a little later on and came up with an index s which tracks till what point the array is sorted, update the next element with candidate element which is higher than its previous
- update the index s, once c is beyond the size of arr.length close code