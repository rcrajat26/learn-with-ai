# 02 Java Collections — diagram manifest (verbatim from the source prompt)

**152 diagrams (D-01 … D-152).** Reproduced verbatim from
`src/metadata/prompts/02-java-collections-prompt.md` so that a resumed run never needs the
prompt to know what a `D-NN` depicts. The `Owner` column is this note set's assignment and
is the only addition.

Rules the manifest assumes:

- One idea per diagram. Prefer more, smaller diagrams over one dense one.
- Where the `Must show` column asks for *frames*, produce that many separate SVGs (or one
  SVG with that many clearly separated, individually labelled panels), each captioned with
  the frame number and what changed since the previous frame.
- Every label, constant and value named in `Must show` must be visible as text in the SVG.
  A diagram that omits a named value does not satisfy the manifest.
- Arrows must be directional and labelled where the direction is not obvious.
- Where the `Type` column says `table`, a Markdown table is the correct rendering and no SVG
  is required.

## Part 1 diagrams

| # | Diagram | Syllabus leaf | Type | Must show | Owner |
|---|---|---|---|---|---|
| D-01 | The pre-1.2 world vs the 1.2 framework | 1.1.1, 1.1.4 | before-after | Left: `Vector`, `Hashtable`, `Stack`, `Dictionary`, `Enumeration` as five disconnected boxes with no shared supertype. Right: `Iterable`→`Collection`→{`List`,`Set`,`Queue`} plus `Map`, with the four standardised layers labelled: interfaces / implementations / algorithms / iteration protocol | framework/01 |
| D-02 | Array covariance hole → `ArrayStoreException` | 1.1.2, 1.1.3 | step-sequence, 3 frames | Frame 1: `Object[] a = new String[2]` with the runtime element type `String` labelled on the array header. Frame 2: `a[0] = 42` with the runtime store check drawn as a gate. Frame 3: `ArrayStoreException` thrown; alongside, `List<String>` with the compile-time rejection instead — label "compile error" vs "runtime error" | framework/01 |
| D-03 | The full interface hierarchy, Java 21 | 1.2.1–1.2.11, 1.2.19 | hierarchy | `Iterable` at root; `Collection`; the Java 21 `SequencedCollection` tier drawn in a distinct colour with a "Java 21" badge; `List`, `Set`, `SequencedSet`, `SortedSet`, `NavigableSet`, `Queue`, `Deque`; `Map` as a separate root with `SequencedMap`, `SortedMap`, `NavigableMap`, and `Map.Entry` nested. Every edge labelled `extends`. Concrete classes shown as leaf boxes in a third style | framework/01 |
| D-04 | Where `java.util.concurrent` grafts on | 1.2.20 | hierarchy | `ConcurrentMap` under `Map`; `ConcurrentNavigableMap` under both `ConcurrentMap` and `NavigableMap` (show the diamond); `BlockingQueue` under `Queue`; `BlockingDeque` under both `BlockingQueue` and `Deque`; `TransferQueue` under `BlockingQueue`; the implementing classes `ConcurrentHashMap`, `ConcurrentSkipListMap`, `ArrayBlockingQueue`, `LinkedBlockingQueue`, `LinkedBlockingDeque`, `LinkedTransferQueue`, `SynchronousQueue`, `DelayQueue`, `PriorityBlockingQueue` | framework/01 |
| D-05 | The abstract skeletons and what each demands | 1.2.16, 3.18.1–3.18.8 | hierarchy | Six boxes: `AbstractCollection`, `AbstractList`, `AbstractSequentialList`, `AbstractSet`, `AbstractQueue`, `AbstractMap`. Each annotated with the abstract methods a subclass must supply (`iterator()`+`size()`; `get(int)`+`size()`; `listIterator()`; `iterator()`+`size()`; `offer`/`poll`/`peek`; `entrySet()`) and what it gives you free | framework/08 |
| D-06 | The iteration-protocol family | 1.2.12, 1.2.13 | hierarchy | `Iterator` with its 4 methods; `ListIterator extends Iterator` with its 6 extra; `Enumeration` standing alone with 2 methods and the `asIterator()` bridge arrow (Java 9); `PrimitiveIterator.OfInt/OfLong/OfDouble`; `Spliterator` with `OfInt`/`OfLong`/`OfDouble` | framework/01 |
| D-07 | Queue's two-form method pairs | 1.2.8, 1.3.9 | table | Rows insert/remove/examine; columns "throws exception" (`add`/`remove`/`element`) and "returns special value" (`offer`/`poll`/`peek`), with the exact exception and the exact special value in each cell | framework/02 |
| D-08 | One deque, two access disciplines | 1.3.10, 1.3.11 | memory-layout | A single array-backed deque drawn once, with stack-facing labels (`push`≡`addFirst`, `pop`≡`removeFirst`, `peek`≡`peekFirst`) on the head end and queue-facing labels (`add`≡`addLast`, `poll`≡`removeFirst`) on both ends; a callout showing `Deque`-as-stack iterating LIFO while `Stack` iterates bottom-to-top | framework/02 |
| D-09 | The `Iterator` state machine | 1.5.2 | state-transition | States: `READY`, `RETURNED`, `REMOVED`, `EXHAUSTED`. Transitions labelled `hasNext()`, `next()`, `remove()`. The illegal edge `remove()` from `READY` and from `REMOVED` labelled `IllegalStateException` | iteration/01 |
| D-10 | `ListIterator` cursor and the `add`/`set` off-by-one | 1.3.19, 1.3.20, 1.5.5 | step-sequence, 4 frames | A list `[A,B,C]` with the cursor drawn *between* elements. Frame 1: cursor at 0, `nextIndex()=0`, `previousIndex()=-1`. Frame 2: after `next()` returns `A`, cursor at 1, `lastReturned=A`. Frame 3: after `add(X)`, `[A,X,B,C]`, cursor at 2, `lastReturned` cleared. Frame 4: `set(Y)` now throws `IllegalStateException` — label why | framework/02 |
| D-11 | Enhanced-for desugaring | 1.5.1 | before-after | Left: `for (String s : list)` and `for (int i : arr)`. Right: the compiler-generated `Iterator` loop for the first and the indexed loop for the second, with the `hasNext`/`next` calls and the `arr.length` bound both drawn | iteration/01 |
| D-12 | Three ways to walk a `Map` | 1.5.11 | step-sequence | Three parallel lanes over the same 4-entry map: `entrySet()` (one table walk, one node touch per entry), `keySet()`+`get(k)` (one table walk plus a second hash+probe per key — draw both traversals), `values()` (one walk, no keys). Label the operation count for each: n, 2n, n | iteration/01 |
| D-13 | `Comparator` chaining and what `reversed()` reverses | 1.6.7, 1.6.8 | flowchart | A pipeline `comparing(Person::last).thenComparing(Person::first).reversed()` drawn as boxes; a bracket around *both* comparators showing `reversed()` wraps the whole chain, not just the last link; contrast with `comparing(last).thenComparing(comparing(first).reversed())` | contracts/01 |
| D-14 | Why `a - b` breaks as a comparator | 1.6.11 | number-line | A 32-bit signed number line with `Integer.MIN_VALUE` and `Integer.MAX_VALUE` marked; the wrap point drawn; a worked pair `a = 2_000_000_000`, `b = -2_000_000_000`, `a - b = -294_967_296` shown with the actual arithmetic, and `Integer.compare(a,b) = 1` beside it | contracts/01 |
| D-15 | Why equal objects must have equal hashes | 1.7.2, 1.7.3 | step-sequence, 3 frames | Frame 1: `put(k1, v)` computes `hash(k1)=A`, lands in bucket `A & 15`. Frame 2: `get(k2)` where `k2.equals(k1)` but `hash(k2)=B` — lands in a different bucket. Frame 3: the entry is present in the table but unreachable; label "the `equals` call is never even made" | contracts/02 |
| D-16 | The mutable-key stranding | 1.7.4, 2.12.9 | before-after | Before: key object with `id=1`, hash 1001, sitting in bucket 9. After: `key.id = 2` mutated in place, hash now 2002 → bucket 2; the object is still physically in bucket 9. Show `contains` probing bucket 2 and missing, `iteration` finding it in bucket 9, and the entry still GC-reachable | contracts/02 |
| D-17 | `getClass()` vs `instanceof` in `equals` | 1.7.6 | before-after | `Point(1,2)` and `ColorPoint(1,2,RED)`. Left panel `instanceof`: `p.equals(cp)` true, `cp.equals(p)` false → symmetry broken, arrow drawn asymmetric. Right panel `getClass()`: both false → symmetric but a `ColorPoint` can never equal a `Point`, Liskov annotation | contracts/02 |
| D-18 | `String.hashCode` computation and caching | 1.7.12 | step-sequence, 3 frames | Frame 1: `"Hi"` → `'H'*31 + 'i'` = `72*31 + 105` = 2337, with the arithmetic shown. Frame 2: the `String` object with `hash` field going from 0 to 2337 and `hashIsZero` false. Frame 3: a string whose hash genuinely is 0 (e.g. `""` or `"polygenelubricants"`), showing `hashIsZero=true` (Java 13+) preventing recomputation | contracts/03 |
| D-19 | `"Aa"` and `"BB"` collide | 1.7.13 | step-sequence | The two computations side by side: `65*31+97 = 2112` and `66*31+66 = 2112`; then the family `"AaAa"`/`"BBBB"`/`"AaBB"`/`"BBAa"` shown as a doubling tree, with the count `2^k` colliding strings of length `2k` labelled | contracts/03 |
| D-20 | `hashCode` of a List vs a Set vs a Map | 1.7.19 | table | Three rows with the exact formula (`31`-fold; sum of element hashes; sum of `key^value`), a worked example over the same three elements, and the consequence (order sensitivity yes/no/no) | contracts/03 |
| D-21 | Type erasure at the class-file boundary | 1.8.1, 1.8.2 | before-after | Source `List<String>` and `List<Integer>` on the left; one `java.util.List` class object plus inserted checkcasts on the right; `ArrayList`'s `Object[] elementData` with the `@SuppressWarnings("unchecked")` cast in `elementData(int)` called out | contracts/04 |
| D-22 | The `Integer` cache | 1.8.7 | memory-layout | The `IntegerCache.cache` array spanning −128..127 as boxed objects on the heap; two `List<Integer>` slots at value 127 pointing to the *same* object; two slots at 128 pointing to two distinct objects; `==` results annotated true/false | contracts/04 |
| D-23 | Boxing blow-up: `int[]` vs `List<Integer>` | 1.8.6, 1.8.8, 3.15.6, 3.15.7 | memory-layout | Top: `int[1_000_000]` = 16-byte header + 4 MB contiguous. Bottom: `ArrayList<Integer>` = 24-byte list + 16-byte array header + 4 MB of 4-byte refs + 1,000,000 × 16-byte `Integer` objects scattered. Totals `4.0 MB` vs `~20 MB` and the 5× ratio labelled | contracts/04 |
| D-24 | The JEP 431 retrofit map | 1.9.1–1.9.5 | hierarchy | Existing types on the left, the new supertype each gained on the right, with the seven new `SequencedCollection` methods listed once and the ten `SequencedMap` methods listed once. `Deque`, `List` → `SequencedCollection`; `LinkedHashSet`, `SortedSet` → `SequencedSet`; `LinkedHashMap`, `SortedMap` → `SequencedMap` | sequenced-collections/01 |
| D-25 | `reversed()` is a view that writes through | 1.9.8 | before-after | `list = [A,B,C]`; `var r = list.reversed()` drawn as a thin wrapper with an index-flipping arrow, not a copy. Then `r.addFirst(X)` and the result `list = [A,B,C,X]`, `r = [X,C,B,A]` — both shown, with the index mapping `r[i] → list[size-1-i]` labelled | sequenced-collections/01 |
| D-26 | `LinkedHashSet.addFirst` moves rather than duplicates | 1.9.7 | before-after | Before: `[A,B,C]` with the insertion-order chain drawn. After `addFirst(B)`: `[B,A,C]` — show the `B` node unlinked from its old position and relinked at the head, size unchanged at 3 | sequenced-collections/01 |
| D-27 | Null policy at a glance | 1.10.1–1.10.5 | table | One row per class (`ArrayList`, `LinkedList`, `ArrayDeque`, `PriorityQueue`, `HashMap`, `LinkedHashMap`, `TreeMap`, `Hashtable`, `ConcurrentHashMap`, `HashSet`, `TreeSet`, `EnumMap`, `IdentityHashMap`, `List.of`, `Collections.singletonList`), columns null key / null value / null element / exception thrown | framework/06 |

## Part 2 diagrams

| # | Diagram | Syllabus leaf | Type | Must show | Owner |
|---|---|---|---|---|---|
| D-28 | `ArrayList` vs `LinkedList` crossover | 2.1.9, 2.1.10, 3.3.9 | cost-curve | x = n (log scale 10 → 1e6), y = time. Two curves for mid-list insert; the crossover region around n ≈ 1000 shaded and labelled; annotations "one vectorised memmove" on the `ArrayList` curve and "n/2 dependent loads, ~1 cache miss each" on the `LinkedList` curve | cost-and-memory/01 |
| D-29 | Cache lines: contiguous array vs scattered nodes | 2.1.10, 3.3.8 | memory-layout | Two 64-byte cache lines holding 16 `ArrayList` refs each; versus 8 `LinkedList.Node` objects at unrelated addresses, each dragging its own cache line. Label "16 elements per miss" vs "1 element per miss" | cost-and-memory/01 |
| D-30 | `modCount` / `expectedModCount` over a loop | 2.2.1–2.2.4 | timeline | A horizontal timeline of iterations; `modCount` and `expectedModCount` as two tracks; the `list.remove(x)` event bumping only `modCount`; the next `next()` call comparing and throwing `ConcurrentModificationException`. Mark exactly where `checkForComodification` runs | iteration/02 |
| D-31 | Why removing the second-to-last element escapes CME | 2.2.5 | step-sequence, 3 frames | List `[A,B,C]`, size 3. Frame 1: `next()` returns `B`, cursor = 2. Frame 2: `remove(B)` → size 2, `modCount` bumped, cursor still 2. Frame 3: `hasNext()` evaluates `cursor != size` → `2 != 2` → false, loop exits silently. Label "no `next()` call, so no comodification check" | iteration/02 |
| D-32 | Three iterator categories | 2.2.11, 2.2.12, 1.5.16 | table | Columns: fail-fast / snapshot (fail-safe) / weakly consistent. Rows: example classes, does it see concurrent updates, can it throw CME, does `iterator.remove` work, memory cost | iteration/02 |
| D-33 | View vs copy vs snapshot | 2.3.1 | before-after | Three panels over the same source list. View: two boxes sharing one backing array, arrows both ways. Copy: two independent arrays, one-way arrow at creation. Snapshot: two independent arrays with a timestamp on the second. A mutation applied to the source in each panel, showing what the second box sees | immutable-collections/01 |
| D-34 | `subList` as an offset window | 2.3.2, 3.1.24, 3.1.25 | memory-layout | Parent `ArrayList` with `elementData` of length 10, size 8. `SubList` object with fields `root`, `parent`, `offset=2`, `size=3`, `modCount`, pointing into the same array. Show `sub.set(0,X)` writing to `elementData[2]` | immutable-collections/01 |
| D-35 | The `subList` memory-retention leak | 2.3.5 | memory-layout | A 1,000,000-element parent array; `big.subList(0,1)` retaining all of it via `root`; beside it, `new ArrayList<>(big.subList(0,1))` retaining a 1-element array. Byte totals on both | immutable-collections/01 |
| D-36 | The three `Map` views | 2.3.6–2.3.9 | memory-layout | One `HashMap` table in the centre; `keySet()`, `values()`, `entrySet()` as three thin view objects around it, each with an arrow labelled with what `remove` does through it (`removes the mapping` / `removes one matching mapping` / `removes the entry`) and what `add` does (`throws UOE`). `entrySet()` shown yielding the actual `Node` objects | immutable-collections/01 |
| D-37 | `TreeMap` range views are range-restricted | 2.3.10 | before-after | A sorted key axis 1..100; `subMap(20, 50)` drawn as a bracket; a `put(60, v)` through the view shown hitting an `inRange` check and throwing `IllegalArgumentException`; a `put(30, v)` shown passing through into the parent tree | tree-map/03 |
| D-38 | Unmodifiable view vs immutable copy | 2.3.15, 2.3.16 | before-after | Left: `Collections.unmodifiableList(src)` — the wrapper rejects writes but `src.add(X)` is visible through it. Right: `List.copyOf(src)` — an independent array, `src.add(X)` invisible. Both mutations drawn and both results shown | immutable-collections/02 |
| D-39 | The five immutability tiers | 2.4.1–2.4.6 | hierarchy | A ladder from Tier 0 (mutable `ArrayList`) to Tier 4 (`List.of`), each rung labelled with its representative factory and the four capabilities it removes (`add`, `remove`, `set`, `reflects source`). `EnumSet` explicitly placed at Tier 0 with a note | immutable-collections/03 |
| D-40 | PECS | 2.5.1–2.5.4 | flowchart | A box "your method" with an inbound arrow from `Collection<? extends T>` labelled PRODUCES (read-only, `add` barred) and an outbound arrow to `Collection<? super T>` labelled CONSUMES (write-allowed, reads give `Object`). `Collections.copy(dest, src)` overlaid on it | contracts/05 |
| D-41 | Why `add` is barred on `List<? extends Number>` | 2.5.9 | step-sequence, 3 frames | Frame 1: `List<Integer> li`, `List<? extends Number> ln = li`. Frame 2: the hypothetical `ln.add(3.14)` shown reaching the `Integer` list. Frame 3: the compiler's rejection, with the capture type `capture#1 of ? extends Number` named | contracts/05 |
| D-42 | `binarySearch` return encoding | 2.6.2 | number-line | A sorted array `[10,20,30,40]` with indices; a hit on 30 returning 2; a miss on 25 with insertion point 2 returning `-(2)-1 = -3`; the inverse formula `insertionPoint = -(result) - 1` written out | utilities/01 |
| D-43 | `rotate` by three reversals | 2.6.5 | step-sequence, 4 frames | `[1,2,3,4,5,6,7]`, distance 3. Frame 1: original with the split at index 4. Frame 2: reverse first part. Frame 3: reverse second part. Frame 4: reverse whole → `[5,6,7,1,2,3,4]`. Each frame shows the exact array contents | utilities/01 |
| D-44 | `nCopies` is n references to one object | 2.6.11 | memory-layout | One heap object; a `CopiesList` view object holding `n` and `element`; contrast with `new ArrayList<>(Collections.nCopies(1_000_000, x))` allocating a 4 MB array of identical refs. Bytes labelled on both | utilities/01 |
| D-45 | TimSort: run detection | 2.8.4, 2.8.5 | step-sequence, 3 frames | An array of 40 elements. Frame 1: natural ascending and strictly-descending runs identified and coloured, descending ones marked for reversal. Frame 2: a short run extended to `minRun` by binary insertion sort, with `MIN_MERGE = 32` and the computed `minRunLength` labelled. Frame 3: the resulting list of runs pushed on the stack | utilities/02 |
| D-46 | TimSort: the merge stack and `mergeCollapse` | 2.8.6 | state-transition | The run-length stack with the two invariants written as inequalities (`runLen[i-3] > runLen[i-2] + runLen[i-1]`, `runLen[i-2] > runLen[i-1]`); three push/collapse steps shown, each labelled with which invariant was violated and which merge fixed it | utilities/02 |
| D-47 | The de Gouw invariant break | 2.8.7 | before-after | The stack state that violates the invariant while `mergeCollapse` believes it holds; the resulting stack depth exceeding the preallocated array; the `ArrayIndexOutOfBoundsException` site; and the JDK fix drawn as "enlarged stack" rather than "corrected invariant" | utilities/02 |
| D-48 | Dual-pivot quicksort partition | 2.8.10 | step-sequence, 3 frames | Frame 1: array with pivots P1 ≤ P2 chosen. Frame 2: the three regions `< P1`, `P1 ≤ x ≤ P2`, `> P2` forming with the moving pointers labelled. Frame 3: final layout with the two pivots in place and the three recursive subranges bracketed | utilities/03 |
| D-49 | Which sort runs, and when | 2.8.10–2.8.12 | decision-tree | Root: element type. Primitive branch → `DualPivotQuicksort` with the sub-branches: insertion sort for tiny arrays, counting sort for `byte`/`char`/`short`, merge-sort fallback on detected structure, heapsort at `MAX_RECURSION_DEPTH`. Object branch → TimSort, with `useLegacyMergeSort` as a side branch to the legacy merge sort. `MIN_ARRAY_SORT_GRAN = 8192` on the `parallelSort` branch | utilities/03 |
| D-50 | Stability, demonstrated | 2.8.14 | before-after | Five records with (name, dept). Sort by name, then by dept. Stable result preserves name order within each dept; the unstable alternative shown scrambling it. Both output lists written out in full | utilities/03 |
| D-51 | `EnumSet` as a bit vector | 2.9.3, 2.9.4, 3.10.9–3.10.12 | memory-layout | A single 64-bit `long` with bit positions labelled by enum ordinal and constant name; `add(WEDNESDAY)` shown as `elements |= 1L << 2`; a union `a.addAll(b)` shown as `elements |= other.elements` with both operands' bit patterns and the result; `complementOf` as `~elements & mask` with the mask shown | specialised-maps/01 |
| D-52 | `IdentityHashMap`'s flat interleaved table | 2.9.8, 3.11.1–3.11.4 | memory-layout | One `Object[]` of length 64 (capacity 32 × 2); key at index `i`, value at `i+1`; three entries placed; a linear probe from a collision walking `i += 2` with wraparound; `DEFAULT_CAPACITY = 32`, `MAXIMUM_CAPACITY = 1 << 29`, and the `size*3 > len` resize condition labelled | specialised-maps/03 |
| D-53 | `WeakHashMap`: the clearing sequence | 2.9.10, 3.11.8–3.11.10 | timeline | Five points on a timeline: (1) key strongly reachable, entry live; (2) last strong ref dropped — entry still present, `size()` still counts it; (3) GC clears the referent and enqueues on `ReferenceQueue`; (4) next map operation calls `expungeStaleEntries()`; (5) entry unlinked. Label the arbitrary-length gap between 2 and 5 | specialised-maps/03 |
| D-54 | The `WeakHashMap` value-holds-key leak | 2.9.11 | before-after | Before: entry with a weak ref to key K and a strong value V; V holds a field pointing at K. Draw the strong path `table → Entry → value V → K` making K strongly reachable, so the weak ref never clears. After: V wrapped in a `WeakReference`, path broken, K collectable | specialised-maps/03 |
| D-55 | `BitSet` word layout | 2.9.17, 2.9.18 | memory-layout | A `long[] words` array; bit `i` located at `words[i >> 6]`, bit `i & 63`; `set(1_000_000)` shown allocating `words[15625]` ≈ 125 KB immediately; `length()` (highest set bit + 1), `size()` (words.length × 64), `cardinality()` (population count) each marked on the same picture with different values | sets/03 |
| D-56 | `floor` / `ceiling` / `lower` / `higher` | 2.10.1 | number-line | Keys `{10, 20, 30, 40}` on an axis, query key 20 and query key 25 both marked. For each query, four labelled arrows to the answer with the relation `≤ / ≥ / < / >` and the returned key or `null` | tree-map/01 |
| D-57 | Sliding-window rate limiter on a `TreeMap` | 2.10.9 | timeline | A `TreeMap<Long,Integer>` of timestamp→count; the window boundary sliding right; `headMap(now - window).clear()` shown evicting the expired prefix; `subMap(now-window, true, now, true)` shown summing the live window. Three time steps | tree-map/01 |
| D-58 | `compute`/`merge` null semantics | 2.11.4–2.11.7 | flowchart | Entry conditions (key absent / present) down one axis and the remapping-function result (null / non-null) across the other; every terminal labelled with the exact outcome: no insertion, insertion, replacement, or *removal*. `merge` with a null value argument shown throwing NPE | utilities/04 |
| D-59 | `removeAll` cost: List argument vs Set argument | 2.12.2 | cost-curve | Two curves over m (argument size): `list.removeAll(list2)` growing as n·m, `list.removeAll(new HashSet<>(list2))` growing as n+m. A worked point at n = m = 10,000 with the operation counts 100,000,000 vs 20,000 written out | sets/02 |
| D-60 | `AbstractSet.removeAll` branches on relative size | 2.12.3 | flowchart | The `size() > c.size()` test; the two branches (iterate `this` calling `c.contains`, or iterate `c` calling `this.remove`); worked with `bigSet.removeAll(smallList)` and `smallSet.removeAll(bigList)` showing the different resulting complexities and the different `contains` implementation exercised | sets/02 |
| D-61 | Stream-to-collection mutability and nullability | 2.3.23, 2.13.9 | table | Rows `Stream.toList()` (16), `collect(Collectors.toList())`, `collect(Collectors.toUnmodifiableList())`. Columns: concrete type today, mutable?, `set` allowed?, nulls allowed?, specified by contract? | immutable-collections/02 |
| D-62 | `groupingBy` with downstreams | 2.13.5 | step-sequence | A 6-element input; `groupingBy(classifier)` forming the buckets; then the same picture with `groupingBy(classifier, counting())`, `groupingBy(classifier, mapping(f, toList()))` and a 3-arg form with `TreeMap::new`, showing what changes at each stage | utilities/05 |
| D-63 | The master choosing decision tree | 2.14.1–2.14.5, 1.4.40 | decision-tree | Root: key-value or elements? Branches on ordered / sorted / unique / bounded / concurrent / both-ends / enum-keyed / primitive-heavy. Every leaf a concrete class. Must reach at least: `ArrayList`, `ArrayDeque`, `LinkedHashSet`, `HashSet`, `TreeSet`, `EnumSet`, `BitSet`, `HashMap`, `LinkedHashMap`, `TreeMap`, `EnumMap`, `IdentityHashMap`, `ConcurrentHashMap`, `ConcurrentSkipListMap`, `CopyOnWriteArrayList`, `PriorityQueue`, `ArrayBlockingQueue`, `LinkedBlockingQueue` | framework/06 |

## Part 3 diagrams

Internals leaves tagged `[SOURCE]` or `[PROVE]` almost always need a step-sequence or
before/after picture. The manifest below assigns one to each such leaf; do not skip any.

| # | Diagram | Syllabus leaf | Type | Must show | Owner |
|---|---|---|---|---|---|
| D-64 | `ArrayList`'s two empty-array sentinels | 3.1.2, 3.1.3 | before-after | Two distinct zero-length `Object[]` objects at different addresses, named `EMPTY_ELEMENTDATA` and `DEFAULTCAPACITY_EMPTY_ELEMENTDATA`. `new ArrayList<>()` pointing at the second, `new ArrayList<>(0)` at the first. Then the first `add` on each: one grows to 10, one grows to 1. Label "the array *identity* is the flag" | array-list/01 |
| D-65 | `ArrayList` growth sequence | 3.1.5–3.1.8 | cost-curve | Step plot of capacity against element count: 10 → 15 → 22 → 33 → 49 → 73 → 109 → 163 → 244, each step annotated with `oldCapacity + (oldCapacity >> 1)` and the resulting value; the `Arrays.copyOf` cost drawn as a spike at each step | array-list/01 |
| D-66 | `add(int index, E)` shift | 3.1.12 | before-after | Array `[A,B,C,D,_,_]`, size 4, `add(1, X)`. The single `System.arraycopy(elementData, 1, elementData, 2, 3)` drawn as one block move; the result `[A,X,B,C,D,_]`; `size` 5, `modCount` incremented | array-list/02 |
| D-67 | `remove(int)` shift and the trailing null | 3.1.13, 3.1.14 | before-after | Array `[A,B,C,D,_,_]`, `remove(1)`. The `arraycopy` left-shift; then `elementData[--size] = null` drawn explicitly, with a callout "otherwise D stays reachable and never gets collected". A second panel contrasting `remove(size-1)` doing zero copying | array-list/02 |
| D-68 | `removeIf`'s deathRow bitset | 3.1.22, 3.1.23 | step-sequence, 3 frames | Frame 1: pass one, predicate evaluated per element, bits set in a `long[]` deathRow — show the bit pattern for a 12-element list. Frame 2: pass two, the single compaction sweep moving survivors left. Frame 3: the tail nulled, `size` updated, `modCount` bumped once. Add a note on `batchRemove`'s `finally` block restoring consistency if the predicate throws | array-list/02 |
| D-69 | `ArrayList.SubList` field wiring | 3.1.24, 3.1.25 | memory-layout | `root`, `parent`, `offset`, `size`, `modCount` on a nested sublist-of-a-sublist; arrows showing `checkForComodification` comparing against `root.modCount`, not `parent.modCount` | array-list/03 |
| D-70 | `Itr` fields during a loop | 3.1.26, 3.1.27 | state-transition | `cursor`, `lastRet`, `expectedModCount` shown at four points: fresh, after `next()`, after `remove()`, after a foreign mutation. Mark exactly where `modCount` is re-read | array-list/03 |
| D-71 | Amortised cost: the potential function | 3.2.4 | cost-curve | Two stacked plots over the same x = number of adds: actual cost (flat 1 with O(n) spikes at each resize) and amortised cost (flat 3). Below, Φ = 2·size − capacity plotted, showing it building to exactly pay for each spike | array-list/04 |
| D-72 | Growth factor 1.5 vs 2: the copy budget | 3.2.2, 3.2.6, 3.2.7 | cost-curve | The geometric series `n·g/(g−1)` evaluated: g = 1.5 → 3n total element copies, g = 2 → 2n. Both plotted. Beside it, peak live memory during the copy: 2.5× vs 3× | array-list/04 |
| D-73 | Why 1.5× lets freed blocks be reused | 3.2.6 | memory-layout | A heap strip. With 2×: successive freed blocks of 16, 32, 64 sum to 112 but the next request is 128 — never satisfiable. With 1.5×: freed 16 + 24 + 36 = 76 can satisfy the next 54-slot request. Show the coalescing explicitly with byte figures | array-list/04 |
| D-74 | `LinkedList` link and unlink | 3.3.3, 3.3.4 | before-after, 2 frames | Frame 1 `linkBefore`: the four pointer writes numbered in order on a 3-node chain. Frame 2 `unlink`: the bypass writes, then `item = null; next = null; prev = null` drawn as three explicit erasures with the "help GC" annotation | linked-list/01 |
| D-75 | `node(int index)` picks its direction | 3.3.2 | step-sequence | A 10-node chain; `get(2)` walking forward 2 hops from `first`; `get(8)` walking backward 2 hops from `last`; the branch `index < (size >> 1)` written on the diagram; worst case at index 5 labelled with 5 hops | linked-list/01 |
| D-76 | Per-element memory: `ArrayList` vs `LinkedList` | 3.3.7, 3.15.13 | memory-layout | `ArrayList`: 4 bytes of array slot per element. `LinkedList.Node`: 12-byte header + 3 × 4-byte refs = 24 bytes. Both drawn to scale, ratio 6× labelled, plus the element object itself shown once for fairness | linked-list/01 |
| D-77 | `ArrayDeque` circular buffer invariant | 3.4.1, 3.4.2, 3.4.15 | memory-layout | A 17-slot ring with `head` and `tail` marked; the one always-empty slot shaded and labelled "why `head == tail` means empty, not full"; `size() = sub(tail, head, 17)` computed on the picture for a wrapped state | array-deque/01 |
| D-78 | `ArrayDeque`: Java 8 mask vs Java 21 helpers | 3.4.3, 3.4.4 | before-after | Left, JDK 8: capacity forced to a power of two, `head = (head - 1) & (elements.length - 1)`, with the bit arithmetic shown. Right, JDK 21: arbitrary capacity 17, `head = dec(head, 17)` with the helper body shown. A prominent VERSION TRAP banner: "power-of-two masking has not been true since JDK 9" | array-deque/01 |
| D-79 | `ArrayDeque.grow` and the un-wrap slide | 3.4.7, 3.4.8 | step-sequence, 3 frames | Frame 1: a full wrapped buffer, `tail < head`. Frame 2: `Arrays.copyOf` to the new capacity — show the wrap now broken across the middle. Frame 3: the head-side segment slid to the end of the new array. `jump = (oldCapacity < 64) ? oldCapacity + 2 : oldCapacity >> 1` written with the actual old and new capacities | array-deque/01 |
| D-80 | Heap array ↔ tree index mapping | 3.5.3 | memory-layout | An 11-slot array beside the binary tree it encodes, indices matched; `parent = (k-1) >>> 1`, `left = 2k+1`, `right = 2k+2` written with a worked k | priority-queue/01 |
| D-81 | `siftUp` | 3.5.5, 3.5.6 | step-sequence, 3 frames | Offer of a small value into a 10-element min-heap; three frames of swap-with-parent, each showing both the tree and the array; the final position. Annotate that `siftUpComparable` and `siftUpUsingComparator` are separate methods for JIT monomorphism | priority-queue/01 |
| D-82 | `siftDown` after `poll` | 3.5.7, 3.5.8 | step-sequence, 3 frames | Frame 1: root removed, last element moved to index 0. Frames 2–3: sink, showing the smaller-child selection before each comparison. Final heap shown as both tree and array | priority-queue/01 |
| D-83 | `heapify` is O(n) | 3.5.9, 3.2.13 | cost-curve | The tree with node counts per level `n/2, n/4, n/8…` and max sift distance per level `0, 1, 2…`; the sum Σ h/2^h converging to 2 drawn as a bar chart with a running total; loop bound `(size >>> 1) - 1` labelled on the tree | priority-queue/01 |
| D-84 | `removeAt` and the iterator's `forgetMeNot` | 3.5.11 | step-sequence, 3 frames | Frame 1: iterator at index 4 in a 9-element heap. Frame 2: `removeAt(2)` moves the last element into index 2 — an element the iterator has already passed. Frame 3: the moved element pushed onto `forgetMeNot` so it is still returned. Label the bug that would occur without it | priority-queue/02 |
| D-85 | `HashMap` anatomy | 3.6.1–3.6.9 | memory-layout | The `Node<K,V>[] table` of 16 slots; three bins: one empty, one with a 3-node chain, one treeified; a `Node` blown up showing `hash`, `key`, `value`, `next`; all six constants listed on the diagram with their values (16, 1<<30, 0.75, 8, 6, 64) | hash-map/01 |
| D-86 | `hash()` — one xor-shift | 3.6.11, 3.6.13 | bit-layout | A 32-bit `hashCode` drawn as boxed bits; `h >>> 16` shown as a shifted copy; the xor result; then `& (n-1)` for n=16 keeping only the low 4 bits, with the high-bit entropy now folded in. Java 7's four-shift version shown beside it for contrast | hash-map/01 |
| D-87 | High-entropy-in-high-bits collision | 3.6.12 | step-sequence | Keys 65536, 131072, 196608 (multiples of 2^16). Frame 1: without the spread, `& 15` gives 0 for all three — one bin. Frame 2: with `h ^ (h >>> 16)`, the indices separate. Actual computed index values on both | hash-map/01 |
| D-88 | `tableSizeFor` | 3.6.15, 3.6.16 | bit-layout | Input 100 as bits; `cap - 1 = 99`; `numberOfLeadingZeros(99) = 25`; `-1 >>> 25 = 127`; `+1 = 128`. Every intermediate shown as a bit pattern | hash-map/01 |
| D-89 | `putVal` control flow | 3.6.19–3.6.21 | flowchart | Table null → `resize()`. Bin empty → `newNode`. Bin head matches → replace. `TreeNode` → `putTreeVal`. Otherwise chain walk with `binCount`, the `binCount >= TREEIFY_THRESHOLD - 1` test, then `++size > threshold → resize()`. Every branch labelled with the source condition | hash-map/02 |
| D-90 | `treeifyBin`'s capacity guard | 3.6.22 | decision-tree | The test `tab.length < MIN_TREEIFY_CAPACITY (64)`; the "resize instead of treeify" branch drawn as the *default* for small tables; the actual treeify branch only above 64. Label why: at small capacity a long bin means the table is too small, not that hashing is bad | hash-map/02 |
| D-91 | Treeify: chain to red-black bin | 3.6.4, 3.6.29, 3.6.30 | before-after | Before: a bin holding a chain of 9 `Node`s, `TREEIFY_THRESHOLD = 8` labelled, table length 64 labelled. After: the same 9 entries as a red-black `TreeNode` bin, with `parent`/`left`/`right`/`prev`/`red` fields shown on one node and the retained `next` linkage drawn as a dashed overlay | hash-map/04 |
| D-92 | `resize()` lo/hi split | 3.6.23, 3.6.24, 3.6.26 | before-after | A bin at index 5 of a 16-slot table holding 5 nodes with their hashes. `(e.hash & 16) == 0` evaluated per node. After: the lo list still at index 5, the hi list at index 21, both preserving relative order. `loHead`/`loTail`/`hiHead`/`hiTail` named | hash-map/03 |
| D-93 | Why exactly one bit decides | 3.6.25 | bit-layout | Old mask `0b01111` (n=16) and new mask `0b11111` (n=32) drawn over the same hash; the single newly-exposed bit highlighted; `that bit == oldCap` written; the two possible destinations `j` and `j + oldCap` | hash-map/03 |
| D-94 | Java 7's concurrent-resize cycle | 3.6.27 | step-sequence, 4 frames | Two threads transferring the same 2-node bin under Java 7's head-insertion transfer. Frame 1: initial `A → B`. Frame 2: thread 1 preempted mid-transfer holding `e = A`, `next = B`. Frame 3: thread 2 completes, producing `B → A`. Frame 4: thread 1 resumes and links `A → B`, closing the cycle; a later `get` shown spinning. Every pointer write numbered | hash-map/03 |
| D-95 | Java 8's quieter concurrent-resize bug | 3.6.28 | before-after | No cycle; instead show a lost entry, a resurrected removed entry, and the NPE site. Label "not an infinite loop, still a bug" | hash-map/03 |
| D-96 | The `TreeNode` inheritance chain and its cost | 3.6.30, 3.15.9, 3.15.11, 3.15.12 | hierarchy + memory-layout | `HashMap.Node` (32 B) → `LinkedHashMap.Entry` adds `before`,`after` (40 B) → `HashMap.TreeNode` adds `parent`,`left`,`right`,`prev`,`red` (56 B). Each box drawn to scale with its field list and byte total, including the header and alignment padding | hash-map/04 |
| D-97 | Poisson bin lengths at load factor 0.75 | 3.6.33, 3.6.34 | cost-curve | Bar chart of `e^-0.5 · 0.5^k / k!` for k = 0..8 with the exact values from the class comment (0.60653066, 0.30326533, 0.07581633, 0.01263606, 0.00157952, 0.00015795, 0.00001316, 0.00000094, 0.00000006); the 8 bar annotated "~6 in 100,000,000" and marked as the treeify point | hash-map/04 |
| D-98 | Treeify/untreeify hysteresis | 3.6.34 | state-transition | Two states LIST and TREE; the edge LIST→TREE labelled "bin size reaches 8 and table ≥ 64"; TREE→LIST labelled "bin size falls to 6 during resize split"; the gap 6..8 shaded and labelled "hysteresis band — prevents thrash" | hash-map/04 |
| D-99 | Sizing a `HashMap` for n entries | 3.6.37, 3.6.38 | step-sequence | `new HashMap<>(100)`: `tableSizeFor(100) = 128`, threshold `128 × 0.75 = 96`, resize at the 97th entry — drawn as a bar hitting the threshold. Then `(int)(100 / 0.75f) + 1 = 134 → tableSizeFor = 256`, threshold 192, no resize. Then `HashMap.newHashMap(100)` (Java 19) doing the same arithmetic | hash-map/05 |
| D-100 | `LinkedHashMap`'s overlay | 3.7.1, 3.7.2, 3.7.15 | memory-layout | The `HashMap` table below, the `before`/`after` doubly-linked chain drawn above it crossing bin boundaries; `head` and `tail` marked; the 2 extra references per entry called out as +8 bytes with compressed oops | linked-hash-map/01 |
| D-101 | `afterNodeAccess` relinks to the tail | 3.7.4, 3.7.8 | before-after | Access-order chain `A→B→C→D`; `get(B)` moving B to the tail giving `A→C→D→B`; the six pointer updates numbered. A second panel showing `containsKey(B)` leaving the chain untouched — the asymmetry called out | linked-hash-map/01 |
| D-102 | The LRU eviction cycle | 3.7.3, 3.7.9, 3.7.10 | step-sequence, 4 frames | A capacity-3 access-order `LinkedHashMap`. Frames: put A, put B, put C; get(A) moving A to the tail; put D triggering `afterNodeInsertion` → `removeEldestEntry` → evicting B (now the head). Show `removeEldestEntry(eldest)` returning `size() > 3` at the decision point | linked-hash-map/01 |
| D-103 | `ReversedLinkedHashMapView` | 3.7.13, 1.9.9 | memory-layout | The same underlying chain with a thin view object that swaps the meaning of `head`/`tail` and of `putFirst`/`putLast`; `reversed().reversed()` shown returning to the original semantics | linked-hash-map/01 |
| D-104 | The five red-black invariants | 3.8.1, 3.8.2 | hierarchy | A 9-node tree coloured red/black with NIL leaves drawn; each of the five invariants numbered and pointed at the place in the tree that demonstrates it; black-height annotated on three distinct root-to-leaf paths, all equal; `h ≤ 2 log₂(n+1)` written with n and h filled in | tree-map/02 |
| D-105 | `rotateLeft` / `rotateRight` | 3.8.5 | before-after, 2 frames | For each rotation: the three nodes and the subtree that changes parent, with the exact pointer writes numbered (child, parent, grandparent, and the `left`/`right` fixups), and the in-order sequence shown unchanged before and after | tree-map/02 |
| D-106 | `fixAfterInsertion` cases | 3.8.6 | step-sequence, 4 frames | Frame 1: red parent, red uncle → recolour and recurse up. Frame 2: red parent, black uncle, zigzag → rotate to straight. Frame 3: red parent, black uncle, straight → rotate and recolour. Frame 4: root forced black. Each frame draws the before and after subtree with colours | tree-map/02 |
| D-107 | `fixAfterDeletion` cases | 3.8.7 | step-sequence, 6 frames | The six cases with the "double black" node marked distinctly in every frame; sibling colour and sibling's children's colours labelled in each; the rotation or recolour applied; the terminating case identified | tree-map/02 |
| D-108 | `deleteEntry` successor swap | 3.8.8, 3.8.9 | before-after | A two-child node being deleted; the in-order successor located (leftmost of the right subtree); the key/value copied up; the successor's own node physically unlinked. Show why the second unlink is always the easy zero-or-one-child case | tree-map/02 |
| D-109 | `buildFromSorted` | 3.8.15 | step-sequence, 3 frames | 15 sorted entries; recursive midpoint selection building a perfectly balanced tree in O(n) with no rotations at all; the level at which nodes are coloured red to fix black-height marked. Contrast the n×O(log n) insert-one-at-a-time path | tree-map/03 |
| D-110 | `TreeMap.Entry` vs `HashMap.Node` | 3.8.17, 3.15.14 | memory-layout | 40 bytes (12 header + 5 refs + colour byte + padding) versus 32 bytes, each field named and sized, both drawn to scale | tree-map/03 |
| D-111 | Skip list levels | 3.8.21, 3.8.22, 3.14.33 | memory-layout | Four levels with `p = 0.25` producing roughly a quarter of the nodes at each higher level; a search path traced from the top-left down and right; a CAS insertion at the base level shown as a single pointer swing, contrasted with a red-black rotation touching three nodes — the reason lock-free is tractable on one and not the other | tree-map/03 |
| D-112 | Set over Map | 3.9.1–3.9.7 | memory-layout | `HashSet` holding a `HashMap`; every value slot pointing at the one shared `PRESENT` object; `add(e)` shown as `map.put(e, PRESENT) == null`. Beside it, `TreeSet`→`TreeMap`, `LinkedHashSet`→`LinkedHashMap` via the dummy-boolean constructor, `ConcurrentSkipListSet`→`ConcurrentSkipListMap` | sets/01 |
| D-113 | `CopyOnWriteArraySet` breaks the pattern | 3.9.8 | before-after | The expected shape (set over map, O(1) add) versus the actual shape (set over `CopyOnWriteArrayList`, `addIfAbsent` scanning n elements then copying the array). Cost annotations O(1) vs O(n) | sets/01 |
| D-114 | `EnumMap` internals | 3.10.1–3.10.5, 3.15.16 | memory-layout | `keyUniverse` array of the enum constants (shared, uncloned, via `SharedSecrets`); `vals` array indexed by ordinal with three occupied and four `null` slots; a null *value* stored as the `NULL` sentinel object, distinguishable from an absent key; `put` shown as `vals[key.ordinal()] = maskNull(v)` with no hashing anywhere | specialised-maps/02 |
| D-115 | `EnumMap`'s reused entry object | 3.10.6, 2.2.15 | before-after | An `EntryIterator` returning the same `lastReturnedEntry` instance each step; a `List<Map.Entry>` built from it shown as n references to one object, all reading the last key/value. The fix (`Map.entry(e.getKey(), e.getValue())` copy) drawn beside it | specialised-maps/02 |
| D-116 | Why enum-keyed `HashMap` is worse | 3.10.14, 1.7.16 | before-after | Two JVM runs of the same program: identity hash codes differ, so bucket assignment and iteration order differ between runs. `EnumMap` shown giving identical ordinal order in both runs | specialised-maps/02 |
| D-117 | `IdentityHashMap` linear probing and `closeDeletion` | 3.11.3 | step-sequence, 3 frames | Frame 1: three keys in a probe chain. Frame 2: the middle one removed, leaving a hole that would break the chain. Frame 3: `closeDeletion` walking forward and back-shifting the displaced entry into the hole. Index arithmetic `i += 2` with wraparound shown | specialised-maps/04 |
| D-118 | `WeakHashMap.Entry` *is* the reference | 3.11.8 | memory-layout | `Entry extends WeakReference<Object>`; the referent slot holding the key; `value`, `hash`, `next` as ordinary strong fields; the `ReferenceQueue` attached. Contrast with a `HashMap.Node` holding a strong key ref | specialised-maps/04 |
| D-119 | `ImmutableCollections.SetN` open addressing | 3.12.6, 3.12.7, 3.12.9, 3.12.10 | memory-layout | 3 elements in a table of `EXPAND_FACTOR × 3 = 6` slots, guaranteeing a free slot; `probe(pe)` returning `i` on a hit and `-(i+1)` on a miss; `SALT32L` from `System.nanoTime()` shown as the per-JVM-run starting offset, with two runs side by side giving two different iteration orders | immutable-collections/04 |
| D-120 | `MapN` interleaved table | 3.12.8 | memory-layout | One `Object[]` of length `2 × EXPAND_FACTOR × n`; key at `2i`, value at `2i+1`; three mappings placed with a probe collision resolved | immutable-collections/04 |
| D-121 | `List.of(a,b)` vs `new ArrayList<>(...)` | 3.12.1, 3.12.2, 3.12.17, 3.15.20 | memory-layout | `List12` as a single object with fields `e0`, `e1` and no array at all; versus an `ArrayList` object (24 B) plus a 10-slot `Object[]` (16 + 40 B) with 8 slots of slack. Byte totals on both | immutable-collections/04 |
| D-122 | `CollSer` serial proxy | 3.12.14, 2.16.6 | step-sequence | `writeReplace()` swapping the immutable collection for a `CollSer` carrying a tag and the elements; the stream contents; `readResolve` rebuilding via the factory; `readObject` on the collection class itself throwing `InvalidObjectException("not serial proxy")` | immutable-collections/04, utilities/06 |
| D-123 | `trySplit` recursion | 3.13.1–3.13.3, 3.13.6 | step-sequence, 3 frames | A 1000-element `ArrayList` spliterator splitting at the midpoint, then each half splitting again — a binary tree of 4 leaves with exact element ranges and `estimateSize()` at each node. Label `SIZED|SUBSIZED` as what makes the exact sizes known and lets fork-join pre-size the output array | iteration/03 |
| D-124 | Good splits vs bad splits | 3.13.5, 3.13.7, 3.13.8, 3.13.9 | before-after | `ArrayList`: perfect midpoint halves. `LinkedList`: `LLSpliterator` walking to build batches — unequal, sequential, no `SUBSIZED`. `HashMap`: table-index-range splits, balanced only if entries are evenly distributed — show a skewed table. `IteratorSpliterator`'s arithmetically growing batches with `BATCH_UNIT = 1024` and `MAX_BATCH = 1 << 25` | iteration/03 |
| D-125 | Spliterator characteristics and what each buys | 3.13.4, 3.13.5 | table | Rows: `ORDERED`, `DISTINCT`, `SORTED`, `SIZED`, `NONNULL`, `IMMUTABLE`, `CONCURRENT`, `SUBSIZED`. Columns: meaning, which pipeline optimisation it enables, which collections report it | iteration/03 |
| D-126 | `ConcurrentHashMap` bin-level concurrency | 3.14.7, 3.14.11, 3.14.12 | memory-layout | A `volatile Node[] table`; an empty bin being filled by `casTabAt` (no lock); a populated bin under `synchronized (f)` on the bin head; two threads working on different bins simultaneously; a reader doing `tabAt` volatile reads through a locked bin without blocking | concurrent-collections/02 |
| D-127 | `spread` and the reserved sign bit | 3.14.9, 3.14.10 | bit-layout | 32 bits; `h ^ (h >>> 16)`; then `& HASH_BITS (0x7fffffff)` clearing the top bit; the three negative special hashes listed with their values and node types: `MOVED = -1` (`ForwardingNode`), `TREEBIN = -2`, `RESERVED = -3` | concurrent-collections/02 |
| D-128 | Cooperative resize with `ForwardingNode` | 3.14.13, 3.14.14 | step-sequence, 4 frames | Frame 1: `table` and `nextTable` side by side, `transferIndex` at the top. Frame 2: thread A CASes a stride of `MIN_TRANSFER_STRIDE = 16` and migrates it, leaving a `ForwardingNode` in each drained bin. Frame 3: a reader hitting a `ForwardingNode` and following it into `nextTable`. Frame 4: thread B, a writer, hitting a `ForwardingNode` and calling `helpTransfer` to claim the next stride instead of blocking | concurrent-collections/02 |
| D-129 | `sizeCtl` states | 3.14.8 | state-transition | States and their encodings: `0` (default, table not yet created), `n` positive (next resize threshold), `-1` (initialising), `-(1 + activeResizers)` (resize in progress). Transitions labelled with the operation that causes each | concurrent-collections/03 |
| D-130 | `size()` via striped counters | 3.14.15, 3.14.16 | memory-layout | `baseCount` plus a `CounterCell[]`; four threads each incrementing a different cell; `@Contended` padding drawn as the space between cells occupying separate 64-byte cache lines; `sumCount()` adding them all; the label "the sum is an estimate — cells can change mid-sum" | concurrent-collections/03 |
| D-131 | Java 7 segments vs Java 8 bins | 3.14.23 | before-after | Left: 16 `Segment` objects, each a `ReentrantLock`-guarded mini `HashMap`, concurrency capped at `concurrencyLevel`, two keys in the same segment blocking each other. Right: one table, per-bin locking, concurrency bounded by bin count. Memory overhead of the segment array called out | concurrent-collections/03 |
| D-132 | `CopyOnWriteArrayList` write path | 3.14.24 | before-after | The `volatile Object[] array`; a writer taking the `ReentrantLock`, `Arrays.copyOf` to a new array of n+1, writing the element, then `setArray` publishing it; two readers still traversing the old array with no lock; `COWIterator` holding its snapshot and `iterator.remove` throwing `UnsupportedOperationException` | concurrent-collections/04 |
| D-133 | When CoW stops being a good idea | 3.14.25 | cost-curve | x = write ratio, y = total cost, one curve per collection size (100 / 10,000 / 1,000,000); the region where `CopyOnWriteArrayList` beats `ConcurrentHashMap.newKeySet()`/`synchronizedList` shaded; the crossover annotated with concrete numbers | concurrent-collections/04 |
| D-134 | `ArrayBlockingQueue` vs `LinkedBlockingQueue` locking | 3.14.28 | before-after | Left: one `ReentrantLock`, `notEmpty` and `notFull` conditions, a single ring buffer — producer and consumer serialise on the same lock. Right: `putLock` and `takeLock` at opposite ends of a linked chain, `AtomicInteger count` shared, cascading signal shown — producer and consumer proceed in parallel | concurrent-collections/05 |
| D-135 | `SynchronousQueue` handoff | 3.14.29, 1.4.32 | timeline | Producer arrives and parks; consumer arrives and the element is handed over directly with no storage; `isEmpty()` returning true throughout. A second lane showing `ThreadPoolExecutor` using it so that an unavailable worker forces thread creation instead of queueing | concurrent-collections/05 |
| D-136 | Michael–Scott queue with lazy tail | 3.14.31, 1.4.34 | step-sequence, 4 frames | Frames showing `tail` lagging one node behind the true last node; a producer CASing `last.next` then attempting the `tail` CAS; another thread helping advance `tail`; and a `size()` traversal walking the whole chain — the O(n), approximate result labelled | concurrent-collections/05 |
| D-137 | Object and array header layout | 3.15.1–3.15.5 | memory-layout | 12-byte object header (8-byte mark word + 4-byte compressed class word) and 16-byte array header (+4-byte length); the 8-byte alignment padding drawn; `Integer` = 12 + 4 = 16 B and `Long` = 12 + 4 pad + 8 = 24 B laid out field by field; a note that `-XX:+UseCompactObjectHeaders` drops 12 to 8 and shifts every figure | cost-and-memory/02 |
| D-138 | 69 bytes to store 8 | 3.15.10 | memory-layout | One `HashMap<Integer,Integer>` entry decomposed: 32 B `Node` + 16 B key `Integer` + 16 B value `Integer` + ~5.3 B amortised table slot at load factor 0.75 ≈ 69 B; the 8 bytes of actual `int` data highlighted inside it; the ratio ~8.6× stated | cost-and-memory/02 |
| D-139 | Node size ladder | 3.15.9, 3.15.11–3.15.14 | table | Rows `HashMap.Node` 32, `LinkedHashMap.Entry` 40, `HashMap.TreeNode` 56, `TreeMap.Entry` 40, `LinkedList.Node` 24, `ArrayDeque` slot 4, `EnumMap` slot 4. Columns: fields, raw bytes, aligned bytes | cost-and-memory/03 |
| D-140 | `BitSet` vs `HashSet<Integer>` | 3.15.18 | cost-curve | Memory against domain size for a dense set: `BitSet` at 1 bit per element, `HashSet<Integer>` at ~50 bytes per element; the ~400× ratio annotated at n = 1,000,000 with both totals in bytes; the sparse regime where the ratio inverts marked | sets/03 |
| D-141 | Collections framework timeline | 3.16.1–3.16.15 | timeline | A horizontal axis from Java 1.0 to Java 25 with a labelled tick per release listed in §3.16, each carrying the types or features it introduced; the three version traps (JDK 9 `ArrayDeque` rewrite, JDK 8 `ConcurrentHashMap` rewrite, JDK 8 `Collections.sort` delegation) marked distinctly | framework/07 |
| D-142 | Hunting a leaking collection in a heap dump | 3.17.1–3.17.6 | flowchart | Symptom (rising old-gen, OOM) → `jcmd GC.heap_dump` → MAT → dominator tree → retained heap → `java.util.HashMap$Node[]` at the histogram top → `collection_fill_ratio` / `map_collision_ratio` queries → the two diagnoses they distinguish (over-allocated `ArrayList` vs bad `hashCode`) → the fix in each case | cost-and-memory/04 |
| D-143 | Extending `AbstractList` for a linked structure is O(n²) | 3.18.4, 3.18.9 | step-sequence | `AbstractList.Itr` calling `get(i)` for each step; on a linked structure each `get(i)` walks i nodes, so a full iteration costs n²/2 hops — drawn as a triangle of work. `AbstractSequentialList` shown as the correct base. Beside it, `AbstractMap.get` scanning the whole `entrySet` | framework/08 |
| D-144 | Extend vs delegate | 3.18.10 | before-after | Left: `class ValidatingList extends ArrayList` overriding `add`; `addAll` shown bypassing the override entirely inside the superclass, so validation is skipped — the exact call path drawn. Right: a forwarding decorator where every method routes through the wrapper, so `addAll` cannot escape | framework/08 |

## Part 4 diagrams

| # | Diagram | Syllabus leaf | Type | Must show | Owner |
|---|---|---|---|---|---|
| D-145 | `MyArrayList` field and class map | 4.1.1–4.1.3, 4.1.9 | hierarchy | The class with `elementData`, `size`, `modCount`, the two sentinel arrays, the nested `Itr`, `ListItr` and `SubList`; an arrow from each to the JDK member it mirrors, so the reader can navigate the real source from the reimplementation | array-list/05 |
| D-146 | `MyHashMap.put` traced against the JDK | 4.3.4–4.3.6 | step-sequence, 4 frames | The same four inserts run through both `MyHashMap` and `java.util.HashMap`, side by side per frame: lazy table allocation, empty-bin fast path, chain append with `==`-before-`equals`, and a resize with the lo/hi split. Any deliberate simplification marked in a distinct colour | hash-map/06 |
| D-147 | Collision DoS, measured | 4.3.14, 3.6.35 | cost-curve | Insert time against key count for 10,000 keys with identical hash codes: the untreeified quadratic curve versus the treeified `n log n` curve, with wall-clock figures on both and the CVE-2011-4858 reference labelled | hash-map/06 |
| D-148 | LRU by hand: `HashMap` + doubly-linked list | 4.6.2, 5.1.17 | memory-layout | The map from key to node; the separate doubly-linked list with sentinel head and tail; a `get` shown as hash lookup then unlink-and-relink-at-tail; a `put` past capacity shown evicting the node after the head sentinel. Every pointer write numbered | linked-hash-map/02 |
| D-149 | Fixed-capacity ring buffer | 4.6.4 | memory-layout | An 8-slot ring with `head`, `tail` and `count`; a full state and an empty state distinguished by `count` rather than by a reserved slot — contrast with `ArrayDeque`'s wasted-slot approach (D-77) | build-it/01 |
| D-150 | `BiMap` two-map invariant | 4.6.6 | before-after | `forward` and `inverse` maps side by side; a `put(k, v)` where `v` is already bound to `k2`, showing the three writes needed to keep both maps consistent, and the state that would result if only one map were updated | build-it/01 |

## Part 5 diagrams

| # | Diagram | Syllabus leaf | Type | Must show | Owner |
|---|---|---|---|---|---|
| D-151 | The `HashMap` one-page answer map | 5.1.1–5.1.9 | hierarchy | A single reference sheet: table → bin → node/tree, with the six constants, `hash()`, index masking, the lo/hi split, treeify at 8 / untreeify at 6 / capacity 64, resize doubling, and the Java 7 loop, each as one labelled box the candidate can walk in 90 seconds. Arrows show the order to say them in | 92-interview-internals |
| D-152 | Version-stale claims vs Java 21 | 5.2.2 | table | Rows: `ArrayDeque` power-of-two masking, `HashMap` Java 7 four-shift rehash, `ConcurrentHashMap` segments, `Collections.sort` copying to an array, "capacity should be prime", `Hashtable` vs `HashMap` "the only difference is synchronization". Columns: what old material says, what Java 21 does, which release changed it, and the one-line answer that shows you know both | 93-drills-and-traps |
