# Learning Preferences

Personal preferences for how prep notes are generated. Read this before generating any day/week notes; apply on top of `conventions.md` and `CLAUDE.md`.

## Active preferences

### Big-O notation — DEFER (added 2026-06-24)
- **Do not focus on Big-O / complexity analysis for now.** The user will learn this later, closer to interview time.
- When a day's plan calls for Big-O (e.g., Day 7 "CTCI Big O second pass", Day 1 Big-O theory), keep complexity mentions minimal:
  - State the final complexity in one line where genuinely needed (e.g., a problem's § solution), no derivation.
  - Skip dedicated Big-O theory deep dives, complexity-derivation walkthroughs, and the full §2 treatment of Big-O as a topic.
  - Do not drill amortized analysis, master theorem, or complexity trade-off comparisons yet.
- **Revisit:** when prep is "near" (the user will signal). At that point, generate a focused Big-O catch-up pass.

### DSA-D days — SKIP for now (added 2026-06-24)
- **Skip all `DSA-D` (DSA-heavy) days for now.** Do not generate them; jump to the next non-DSA day type the user picks.
- Also skip the DSA portions embedded in mixed days (e.g., the "DSA review (1h): re-do 2 tree problems" sub-block on HLD-D days, the DSA self-mock on Review-D days). Cover the non-DSA content fully.
- Generation order is now user-directed, not strictly sequential. Confirm the next day when ambiguous.
- First non-DSA day generated under this preference: Day 42 (HLD-D — System Design starts).
- **Revisit:** when the user wants to resume DSA grinding (they will signal).